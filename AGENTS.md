# AGENTS.md

This file provides guidance to Codex when working with code in this repository. It is a verbatim mirror of CLAUDE.md — edit CLAUDE.md first, then copy.

## Commands

All commands run from the repo root via **Bun workspaces**:

- `docker compose up -d postgres` — start Postgres before anything else touches the DB
- `bun run dev` — runs `apps/web` (:3001) + `apps/api` (:3000) in parallel
- `bun run dev:web` / `bun run dev:api` — single workspace
- `bun run build` — `next build` + `bun build` for api
- `bun run typecheck` — `tsc --noEmit` across every workspace
- `bun run test` — **full test suite** (FSRS units + API integration). Auto-pushes schema to the test DB first.
- `bun run test:unit` / `bun run test:api` — narrow to one package
- `bun run db:push` — sync Drizzle schema to Postgres (dev, non-versioned, `--force`)
- `bun run db:push:test` — same, but against `TEST_DATABASE_URL`
- `bun run db:generate` / `db:migrate` — versioned migration flow for prod
- `bun run db:studio` — drizzle-kit Studio

Ports: web **3001**, api **3000**, postgres **5432**. Swagger UI at `http://localhost:3000/docs` — use it instead of curl when poking new endpoints.

Prereqs: `bun >=1.3`, Docker, a root `.env` (copy from `.env.example`). CI gates: `bun run typecheck` + `bun run test` + clean builds. No linter configured.

## Production

- **Versioned migrations**: edit schema → `bun run db:generate` → commit the `packages/db/src/migrations/*.sql` files. Apply via `bun run db:migrate:apply` (calls the programmatic `packages/db/src/migrate.ts`). CI runs `db:migrate:apply:test` before `bun run test`. Dev loop still uses `db:push --force` for speed.
- **Rate limiting**: `apps/api/src/rate-limit.ts` — IP-based, in-memory. Sign-in = 5/min, sign-up = 5/hour, forgot-password = 3/hour. Disabled under `NODE_ENV=test`. For multi-instance prod, swap the `Map` for Redis — the `rateLimitCheck` interface is the same.
- **Email / password reset**: `packages/auth/src/email.ts` picks `ConsoleEmailProvider` (dev, logs to stdout) or `ResendEmailProvider` (set `RESEND_API_KEY` + `EMAIL_FROM`). BetterAuth's `emailAndPassword.sendResetPassword` calls `sendEmail`; the web side has `/auth/forgot-password` → token email → `/auth/reset-password?token=…`. All calls go via bare `fetch` on the client (not the BetterAuth SDK) so version bumps don't rename methods under us.
- **GDPR**: `GET /profile/export` returns a full JSON dump of the user (profile, decks, cards, reviews, achievements). `DELETE /profile` (body: `{ confirmEmail }`) removes the user — FK cascades clean everything else. Covered by 5 integration tests.
- **Health**: `GET /health` runs `SELECT 1` via `dbPing()` and returns `{ ok, db: { ok, latencyMs } }` or 503. Docker healthcheck calls this every 30 s.
- **Graceful shutdown**: SIGTERM/SIGINT → `app.stop()` → `closeDb()`. The api logs `[api] bye.` before exit; Docker `stop_grace_period` should be ≥ 5 s.
- **Prod builds**:
  - API: `bun build src/index.ts --target bun --outdir ./dist` (currently ~2.75 MB bundle). Served by `bun dist/index.js`.
  - Web: `next build` with `output: 'standalone'` + security headers (CSP, X-Frame-Options, Permissions-Policy, Referrer-Policy). CSP whitelists `NEXT_PUBLIC_API_URL` for `connect-src`.
- **Docker**: `apps/api/Dockerfile` (multi-stage Bun 1.3-alpine), `apps/web/Dockerfile` (multi-stage Bun → node:22-alpine for the Next standalone runtime), `docker-compose.prod.yml` orchestrates api + web + postgres + a one-shot `migrate` service. Build from repo root so workspace symlinks resolve.
- **CI**: `.github/workflows/ci.yml` — postgres-17 sidecar, bun-setup@v2, install → `db:migrate:apply:test` → typecheck → test → build on every PR.
- **Structured logging**: `apps/api/src/logger.ts` — pino, dev via `pino-pretty`, prod emits one JSON line per log. `LOG_LEVEL` overrides default (`debug` dev / `info` prod / `silent` test). Redacts `authorization` / `cookie` / `password` / `token` automatically. Every incoming request gets a child logger bound to `{ requestId, method, path }` via `.onRequest`; handlers pull it out of `store.log`.
- **Request ID**: `apps/api/src/app.ts` honours upstream `x-request-id` header (set by LB / reverse proxy) or generates a UUID. Echoed back in the response and exposed via `Access-Control-Expose-Headers` so the web client can surface it in error toasts. Covered by 2 integration tests.
- **Pagination**: `GET /cards` is cursor-paginated (`?limit=500&cursor=<iso>`), ordered by `created_at DESC`. Response shape `{ items, nextCursor }` — next page starts after that cursor; `nextCursor: null` means end of stream. `DEFAULT_CARDS_PAGE=500`, hard cap `MAX_CARDS_PAGE=1000`. Store bootstraps the first page only; deeper fetch is on-demand. Covered by 1 pagination integration test.
- **Loading states**: `apps/web/src/components/ui.tsx` exports `NNSkeleton` (shimmer via `@keyframes nn-shimmer` in `globals.css`). Home and Review substitute the full screen for a layout-matching skeleton while `!bootstrapped` — no more opacity dimmer.

## AI / RAG (knowledge base + grounded chat)

A read-only, source-agnostic RAG foundation: a derived `kb_chunk` table (pgvector, HNSW cosine) indexed from `cards.render_text`, plus persisted `conversations` / `messages`. Scoped by `user.id`. Chat is strictly read-only over the user's own content — no card authoring, no SRS writes, no schedule moves.

- **Env vars (all optional — `apps/api/src/env.ts` `env.ai`):** `OPENAI_API_KEY`, `EMBEDDING_MODEL` (default `text-embedding-3-small`), `EMBEDDING_DIM` (default `1536`), `CHAT_BASE_URL` (default `https://api.openai.com/v1`, OpenAI-compatible), `CHAT_MODEL` (e.g. `gpt-4o-mini`), `CHAT_API_KEY`, `CHAT_MODELS` (optional allow-list for the per-turn reasoning/model picker), `INDEXING_ENABLED` (default `true`). Server-only secrets — never `NEXT_PUBLIC_*`. See `.env.example`.
- **Degrade, never crash:** none of the above use `required()`. Two INDEPENDENT derived flags gate behavior: `embeddingEnabled = Boolean(OPENAI_API_KEY) && INDEXING_ENABLED !== 'false'` (gates the index queue + write-hooks) and `chatEnabled = Boolean(CHAT_API_KEY)` (gates the SSE chat endpoint). With no keys the index queue no-ops and `/chat` shows a setup notice; the rest of the app is unaffected. `GET /ai/status` (auth) reports `{ embeddingEnabled, chatEnabled, webSearchEnabled, embeddingModel, chatModel, embeddingDim, degraded, models }` — `models` is the parsed `CHAT_MODELS` allow-list (`{ id, label, default }[]`, `[]` when unset).
- **Reasoning/model selector (`CHAT_MODELS`):** an OPTIONAL CSV allow-list for a per-turn model picker — each entry is `model` or `model|label`, the FIRST entry is the default (e.g. `CHAT_MODELS="gpt-5.5high|Deep,gpt-5.5none|Fast"`). Parsed by `parseChatModels` in `@neuronexus/shared` (`chat-models.ts`: trims, drops blanks, de-dups by id, first=default; empty/undefined ⇒ `[]`). Surfaced as `{ id, label, default }[]` via `GET /ai/status` (NEVER the key/base URL — only labels/ids leave the server). The web composer renders a compact picker only when `models` is non-empty; the choice persists in `localStorage` (`nn:chat:model`) and is re-validated against the live allow-list on load (a stale value silently falls back to the default). The chosen model rides the stream/resume/regenerate body (`model?`) → validated server-side against the allow-list (unknown → 400 `invalid_model`) → threaded `runAgentTurn(model) → chatStreamAgentic({ model })` on EVERY step. Unset ⇒ picker hidden, chat behaves exactly as the single `CHAT_MODEL`. No new required env, no migration.
- **Stats endpoints (`/stats`):** `apps/api/src/modules/stats.ts` — `GET /stats/forecast?days=&deckId=` (due forecast: sparse day-buckets `date_trunc('day', due)` over the NEXT N days (clamp 1..90, default 30) + a SEPARATE `overdueCount` backlog (due before start of today, disjoint from buckets); excludes `suspended` and `state='new'` — a new card's `due` is fictitious) and `GET /stats/retention?days=&deckId=` (TRUE retention as f(interval): `LAG(reviewed_at) OVER (PARTITION BY card_id)` gap buckets `<1d/1d/2-3d/4-7d/8-15d/16-30d/31-90d/90d+`; first review of a card excluded; the time window filters OUTSIDE the windowed CTE or LAG would lose predecessors). Both helpers live in `progress-stats.ts` (`dueForecast`/`retentionCurve`), are user-scoped (first conjunct), resolve `deckId` → subtree via `descendantIds`, foreign deckId → empty scope. The stats screen renders both as new panels (zero-fill the sparse forecast buckets client-side; hide retention buckets with n<5). Day bucketing is UTC (`date_trunc` server-side) — accepted V1 tradeoff, consistent with the studyStats heatmap.
- **Semantic graph (`kb_chunk`-powered, NO runtime embedding calls):** `apps/api/src/ai/similar.ts` (`similarCards`: the card's STORED chunk embedding is the query vector → `retrieve()` → aggregate MAX score per neighbour card, source card excluded) and `apps/api/src/ai/semantic-edges.ts` (`semanticEdges`: one k-NN-join SQL — user-scoped src CTE capped `maxNodes=1000` newest-first + `CROSS JOIN LATERAL` ANN probe `LIMIT k*2` + undirected dedup `GROUP BY LEAST/GREATEST` + `MAX(score)`; suspended filtered OUTSIDE the lateral so the planner can use HNSW; on small corpora the planner picks a seq scan inside the lateral = exact-scan fallback, verified by EXPLAIN). Routes in `apps/api/src/modules/semantic.ts`: `GET /cards/:id/similar` (ownership check first → 404 foreign/missing; no chunk → 200 `{ items: [], reason: 'not_indexed' }`) and `GET /graph/semantic-edges` (`{ edges, nodes, reason? }`). Both deliberately do NOT gate on `embeddingEnabled` — they work from stored vectors alone (tests insert fixtures directly via `insertChunkFixture` in `tests/helpers.ts`, proving AI-client independence). Vectors never leave the server. Web: `apps/web/src/components/similar-cards.tsx` (`SimilarCardsPanel` + module-level cache) renders in the cards-browser edit dock (desktop right rail / mobile section; `onOpen` → `refetchCard` upserts cards outside the 500-row mirror) and a reviewer drawer (icon appears only AFTER reveal — no answer spoilers; Esc closes drawer before exiting zen/home). The graph screen fetches `/graph/semantic-edges` on mount; `buildGraph(..., edgesOverride?)` swaps tag edges for semantic ones (default `semantic` when data exists, degrade to `tags`; user choice persists in `localStorage('nn:graph:edges')`; segment control hidden when `not_indexed`); `NodeDetail` shows `{pct}% similar` for semantic edges.
- **Agentic progress read-tools:** THREE read tools backed by `apps/api/src/modules/progress-stats.ts`. `due_forecast({ days?, deckId? })` is the forward-looking counterpart to `study_stats` (per-day due counts + overdue backlog, same deck-subtree rules; routed in the prompt as FUTURE vs study_stats' past). The original two READ tools are backed by the same module (a tested, user-scoped `GROUP BY date_trunc('day', reviewed_at)` aggregation helper — `user_id` is the mandatory FIRST conjunct on every query). `card_progress(cardId)` returns one card's FSRS state (`state/reps/lapses/due/stability/difficulty/lastReview/suspended`) + its last-N review history (scoped by BOTH `cardId` AND `userId`). `study_stats({ scope:'global'|'deck', deckId?, days? })` returns review count, retention % (`rating >= 3`), study minutes, and a day-bucketed heatmap for the window (`days` clamped 1..365, default 30); `scope:'global'` also reports streak/level/xp from `profile`. Deck scope includes the subtree via the EXPORTED server walker `descendantIds` in `apps/api/src/modules/cards.ts` (NOT the web-only `lib/decks.ts`); a foreign/un-owned `deckId` resolves to an EMPTY scope, never a global fallback. Both follow the `search_cards` pattern (`kind:'read'`, return `ToolResult`, never throw into the loop, registered in `buildToolRegistry`, compact text well under `TOOL_RESULT_MAX_CHARS`); empty history renders "no reviews yet", not an error. `buildAgentSystemPrompt` routes progress-meta questions ("что я заваливаю?", "сколько я занимался?") to these tools while keeping conversation-meta + small-talk tool-free.
- **Chat UX (rename / stop / regenerate / copy / jump-to-card / deck scope):** `PATCH /chat/conversations/:id` renames a thread (user-scoped 404, title 1..200 → 400 on violation); the web thread list does inline rename + a hand-rolled relative "updated N ago" timestamp (no dep). Stop uses an `AbortController` in `streamChat`/`resumeChat`/`regenerateChat` (`apps/web/src/lib/chat-stream.ts`) — the `AbortError` is swallowed (never `onError`). The user row commits BEFORE streaming but the assistant turn commits only at end-of-stream, so an aborted/torn turn leaves a TRAILING user row with no answer; a recoverable "stopped — regenerate?" affordance shows both live AND on reload (trailing-user detection in `reconstructMessages`/render). `POST /chat/conversations/:id/regenerate` is delete-tx THEN replay-tx (NOT atomic — its failure tail is identical to the abort tail, recovered by the same affordance); it replays the LAST user message with the CURRENT model. Copy strips `[card:<id>]` tokens then `navigator.clipboard.writeText` + a toast. Jump-to-card: a chat citation's "open card" affordance `router.push('/cards?focus=<id>')`; `cards-browser.tsx` reads `?focus=` and opens the bottom edit dock on that card (then clears the param). The composer's optional deck-scope picker sends a turn-level `deckId?` that constrains `search_cards` retrieval to that deck's subtree. The empty state of a NEW conversation shows up to 4 suggested-prompt pills built purely from the store mirror (due count, biggest deck name) — zero extra fetches, i18n via `chat.suggested.*`; a pill calls `sendContent(text)` directly (NOT `setDraft + send` — `send()` reads the stale draft from its closure; `send` is now a thin `sendContent(draft.trim())` wrapper).
- **Reviews index for deck-scoped stats:** migration `0012_exotic_morgan_stark.sql` adds `reviews_user_deck_reviewed_idx (user_id, deck_id, reviewed_at)` — deck-scoped `studyStats`/`retentionCurve` pick it up automatically, no query changes.
- **Embeddings are DERIVED, disposable index.** `kb_chunk` is rebuildable at any time from `cards.render_text` (like `cards.render_text` is itself a derived search cache). Deleting the source card cascades the chunk (FK on `kb_chunk.card_id`). Rebuild via `POST /ai/reindex` (user-scoped) or the `backfill.ts` script. `kb_chunk.source_hash` = `hash(render_text + embeddingModel)` so a no-content `updatedAt` bump (e.g. `forget`/`setDue`/bulk `move`) does NOT trigger a paid re-embed.
- **pgvector prerequisite (prod):** the production Postgres must have the `vector` extension available. The bundled `docker-compose.prod.yml` uses `pgvector/pgvector:pg17`; for managed Postgres (Neon/Supabase/RDS) confirm pgvector is enabled before applying the `0010_*` migration (it runs `CREATE EXTENSION vector` as its first statement). `CREATE EXTENSION` is **per-database** — both `neuronexus` and `neuronexus_test` need it. Locally the `predb:push` / `predb:push:test` hooks in `packages/db/package.json` run `ensureVectorExtension()` BEFORE `drizzle-kit push --force` (a `beforeEach` ensure is too late — push is a separate process). Index type is **HNSW** (`vector_cosine_ops`) — no training/`lists` tuning, good recall on a small/growing corpus.
- **Agentic environment (4-pack milestone — threads/transcript/agent-backend/composer):**
  - **Token usage (C1):** `chatStreamAgentic` sends `stream_options: { include_usage: true }` (kill-switch `CHAT_STREAM_USAGE='false'` for gateways that reject it); the usage chunk arrives with `choices: []` and is parsed BEFORE the `choices[0]` guard in `openai-client.ts`. The loop accumulates across all steps; `persistTranscript` stamps `messages.model` on every assistant row and `messages.usage` (jsonb `{promptTokens, completionTokens, totalTokens}`) on the LAST assistant row of the buffer — a suspended turn parks usage-so-far on the pending tool_calls row, the resume continuation persists its own (sum over rows = turn totals). SSE `{type:'usage'}` before `done`; no frame/null column when the provider omits usage. Web: dim `model · N tok` badge under finished answers (label via `status.models`).
  - **Parallel read tools + mixed batches (C2):** a read-tool batch executes CONCURRENTLY (`Promise.all`); `tool_call` frames go out up-front in batch order, `tool_result` frames per-completion; everything order-sensitive (citations, caps, char budget, persisted `role:tool` rows) applies AFTER the join in batch order. A MIXED batch executes reads first as their own fully-answered tool_calls row, THEN pauses on the first write as a second row (before C2 the reads were silently dropped). Dangling-guard/resume idempotency untouched.
  - **Auto-titling (C3):** `apps/api/src/ai/title.ts` + the non-streaming `complete()` member on `AiClient` (NEVER `chatStreamAgentic` — scripted test fakes would lose a turn; fakes without `complete` skip titling gracefully). On `/stream` and `/regenerate` over a `title IS NULL` conversation, the title generates concurrently with the turn (timeout `CHAT_TITLE_TIMEOUT_MS`, default 10 s — slow self-hosted gateways routinely exceed 5 s), persists via `UPDATE … WHERE title IS NULL` (manual rename wins) and emits SSE `{type:'title'}` before `done`. The web client now creates conversations WITHOUT a title (first-message slice is a local placeholder).
  - **Pinned threads (C4):** `conversations.pinned`; PATCH `/chat/conversations/:id` takes `{ title?, pinned? }` (empty body → 400 `nothing_to_update`; `updatedAt` bumps ONLY on title change). List orders `pinned DESC, updated_at DESC`. The thread rail (extracted `components/chat/thread-rail.tsx`) renders search + date groups (Pinned/Today/Yesterday/7 days/Older, LOCAL midnights — pure helpers in `lib/chat-threads.ts`).
  - **Agent instructions (C5):** `profile.agent_instructions` (PATCH `/profile`, cap 2000, whitespace-only → NULL) → injected into `buildAgentSystemPrompt` as a guardrailed `<user_instructions>` section (preferences only — can never override grounding/citation/confirm-before-write). Settings screen has a save-on-blur textarea section.
  - **Context auto-compression (C6):** `apps/api/src/ai/compress.ts` — beyond `CHAT_COMPRESS_THRESHOLD` (80) history rows, all but the last ~`CHAT_COMPRESS_KEEP` (30) replay as ONE summary system note; the cut walks BACK to the nearest user row (a tool cluster is never split). Summary + boundary cached on `conversations.summary/summary_upto` — an up-to-date cache costs ZERO extra calls; refresh merges the previous summary + newly-aged rows via `complete()` (timeout `CHAT_SUMMARY_TIMEOUT_MS`); ANY failure → verbatim history.
  - **Card mentions (C7):** stream/regenerate body `mentionedCardIds?` (uuid[], max 8) → user-scoped resolve (foreign ids silently dropped) → snapshot persisted on the user row's `messages.mentions` jsonb (`{cardId, front ≤200, deckName?}`); the STORED content stays clean — the model-facing `<mentioned_cards>` block is appended at history-build time (`appendMentionBlock`). Regenerate keeps the stored snapshot unless the body carries new ids. Composer: `@` opens a deck/card popover (`lib/chat-mentions.ts` pure triggers — `a@b` is NOT a trigger; `/` only at draft start); a deck pick sets the existing per-turn scope, a card pick becomes a removable chip.
  - **Confirm previews (C8):** `edit_card.dryRun` adds `fieldDiffs` (changed fields only, values capped 300) + `tagsChange`/`deckChange`/`suspendedChange` (args-only for count-neutral moves — no DB); `create_card.dryRun` adds `proposedFields`. Shared shape `ConfirmImpact` (kb-chunk.ts) = `ToolImpact` alias. Web renders −rose/+lime diff rows in the confirm card (`components/chat/confirm-diff.tsx`); absent payload (old rows) degrades to blast-radius-only.
  - **Per-card confirm decisions (batch create_card):** the confirm card is an EDITOR, not just a preview — each proposed card can be excluded, inline-edited (textarea per field, built from the ORIGINAL args — full values, the capped impact preview is display-only — so it works live AND after reload), plus an optional `feedback` note to the model. **Batch UX is a one-card-at-a-time WIZARD** (user request — a 10-card wall was unusable): «Карточка N из M» with Accept/Exclude/Back per card (`nextUndecidedIndex` drives navigation — wraps to remaining undecided, re-deciding overwrites), then a REVIEW step (clickable per-card summary rows → jump back, feedback note, blast-radius line) firing ONE resume with the accumulated `cardSelections`. The agent protocol is untouched — decisions accumulate client-side. Single-card proposals keep the flat editor. Resume body += `cardSelections: [{index, include, fieldValues?}]` + `feedback` (web sends ONLY excluded/actually-edited entries — pure helpers `createCardDraft`/`buildCardSelections` in chat-activity.ts). Server (`applyCreateCardSelections` in ai.ts): merges selections into the pending args at execute time — the PERSISTED assistant row keeps the model's original proposal; the tool-result text is augmented («User excluded N… / edited M… / User feedback: …») so the model learns what changed; ALL cards excluded ⇒ the apply DEGRADES to a reject; reject carries `feedback` inside the `user_rejected` JSON. Single-card proposals are addressed as index 0. Apply button shows «Применить (N)» and disables at N=0.
  - **Rich-content generation guidance (prompt):** card fields render Markdown — the write-workflow section now tells the model to use GFM pipe tables, fenced code blocks, ```mermaid diagrams and KaTeX via `\(...\)` / `\[...\]` (NOT `$...$` — MATH_RE is Anki-style). Images: ONLY real `/m/<uuid>` media tokens as `![](/m/<uuid>)`; `attachmentTextBlock` now surfaces every chat-attached image's token (`[image … — embeddable in a card field as ![](…)]`, for both visible image parts and placeholders) so the model can embed user screenshots into cards. Never external image URLs (sanitizer strips them).
  - **Batch create_card (`cards: [...]`):** `create_card` accepts EITHER a single `fieldValues` OR `cards: [{fieldValues, tags?}, …]` (cap `CREATE_CARD_BATCH_MAX=20`) — the whole batch is ONE tool call → ONE confirmation → ONE transaction on apply (a failing entry rolls back all). The note type resolves ONCE per call; batch-entry errors are index-addressed (`cards[1]: …`) and take the validate-before-pause path. dryRun: single → `proposedFields` (unchanged), batch → `ConfirmImpact.proposedCards` (per-card `fields` arrays); web `confirmDiffRows` tags rows with `cardIndex` and `ConfirmDiff` renders «Card N» dividers. Activity label switches to `chat.tool.create_card_batch[_nodeck]` («Черновики карточек (N)»); `applySummaryFrom` counts `args.cards`. The prompt now says to BATCH multi-card requests into one call (the old «one at a time» rule made the agent refuse multi-card creation — never reintroduce it). Per-entry `tags` override the top-level `tags` fallback.
  - **Live tool_result summaries:** successful `tool_result` SSE frames carry `summary` = the capped model-facing text (`capToolResult`) — both the read-batch path and the resume-apply path — so EVERY finished step in the activity feed is expandable live, identical to what reload reads back from the persisted `role:tool` row. Errors keep `summary = error` as before; web needed no changes (the step body already rendered `result` when present).
- **Deep research (`fetch_page` + Exa):** the agent can READ full web pages, not just Brave snippets, and slice documentation into card batches.
  - **`fetch_page` read tool** (`tools.ts`, registry slot between `web_search` and the writes): args `{ url, offset? }`; returns `«Title» — url` + a `[chars X–Y of Z — call again with offset=Y]` header + a ~3200-char text slice (sized to fit under `TOOL_RESULT_MAX_CHARS` without blind truncation) + a `Links on this page:` block (first slice only, ≤12) so the model can crawl into subpages. Backed by `apps/api/src/ai/page-reader.ts` behind a 15-min/32-entry in-memory cache — offset slices of one page never re-crawl (or re-bill Exa).
  - **Two reader backends** (mirror of the web-search provider pattern, seams `__setPageReaderForTests` / `__setPageFetchForTests` / `__setDnsLookupForTests`): `ExaPageReader` (POST `api.exa.ai/contents`, `text.maxCharacters=10000` — Exa's hard cap, `extras.links`, `livecrawlTimeout=FETCH_PAGE_TIMEOUT_MS`; Exa crawls on THEIR network ⇒ no SSRF surface) preferred when `EXA_API_KEY` is set; else `DirectPageReader` — hand-rolled fetch with SSRF guards: http(s) only, no userinfo, blocked host suffixes (.local/.internal/.localhost/.home.arpa), private/reserved IP literals AND DNS-resolved addresses rejected (v4 ranges + v6 incl. the WHATWG hex form of v4-mapped `::ffff:a00:1`), redirects `manual` with per-hop re-validation (3 max), 10s timeout, 2MB byte cap, text-ish content-type allow-list, hand-rolled HTML→text (+title+links). Known accepted v1 gap: DNS TOCTOU/rebinding on the direct path — the Exa path avoids the class.
  - **Exa as web-search provider:** `ExaWebSearchProvider` (POST `api.exa.ai/search`, `type:'auto'`, `contents.text.maxCharacters=300` for snippets) — `getWebSearchProvider()` precedence: injected → Exa → Brave; `webSearchEnabled` now = `Boolean(BRAVE || EXA)`.
  - **Env (all optional):** `EXA_API_KEY`, `CHAT_FETCH_PAGE` ('false' kills the tool), `FETCH_PAGE_TIMEOUT_MS` (10000), `FETCH_PAGE_MAX_BYTES` (2MB), `TOOL_RESULT_BUDGET_FACTOR` (default **8**, replaces the old hardcoded ×4 turn budget — deep-research turns read several slices before the loop forces an answer).
  - **Prompt:** `buildAgentSystemPrompt` opts += `fetchPageEnabled?` → a fetch_page routing line + a «Deep research → flashcards» workflow (fetch → continue via offset/links → match deck via list_decks → atomic cards → `create_card` `cards:[...]` batches, work in parts on long material) + fetch_page added to the untrusted-data security line; all of it ONLY when the tool is offered.
  - **Web:** activity label `chat.tool.fetch_page` (+`_n` plural for runs) with a compact scheme/www-stripped URL as argMono (never raw JSON), `doc` icon, `/research` slash command (SLASH_COMMANDS + `slash.researchTemplate` in both locales). Tests: `page-reader.test.ts` (SSRF matrix, HTML extraction, Exa mapping, cache), `agent-fetch-page.test.ts` (slice/links frames, cached offset continuation, graceful reader failure, forced-off ⇒ unknown tool, research-mode caps/prompt).
  - **Deep-research MODE (composer toggle):** a sticky pill toggle next to the model/deck pickers (`localStorage('nn:chat:research')`, hidden unless `/ai/status.fetchPageEnabled` — the status route now exposes that flag). When on, EVERY turn rides `research: true` in the stream/resume/regenerate body (resume too, so a confirm-split research turn keeps its mode). Effective server-side only when `isFetchPageEnabled()`: adds a `<deep_research_mode>` prompt section (read-thoroughly-then-deliver directives) AND raises the loop caps per turn — `RESEARCH_MAX_STEPS` (default 16, vs `AGENT_MAX_STEPS` 8) + `RESEARCH_TOOL_RESULT_BUDGET_FACTOR` (default 16, vs 8). The composer placeholder swaps to a research prompt while active. `runAgentTurn` takes `research?` and computes `maxSteps`/`toolBudget` per turn — the module-level consts are only the non-research defaults.
  - **Transcript/composer UX:** smart stick-to-bottom (`lib/use-stick-to-bottom.ts` — auto-follow only near bottom, floating jump pill otherwise), LOCAL day separators + hover `HH:MM` times (`.nn-msg-time`), code-block copy buttons via post-render DOM decoration (`components/chat/code-copy.ts` — final renders only, sanitizer untouched), readable tool args (`formatToolArgs` — never `[object Object]`, full UUIDs masked to 8 chars), write/SRS tool labels in the activity feed, per-thread draft persistence (`nn:chat:draft:<id|'new'>`, written on change — no effect races), ONE queued follow-up while streaming (auto-sent on settle, held during pending confirmations, returned to the draft on Stop), deep link `/chat?thread=<id>` (kept in the URL; `chat/page.tsx` wraps NNChat in `<Suspense>` for `useSearchParams`).
- **Agent hardening + attachments (robustness milestone):**
  - **Per-conversation turn lock:** `/stream` + `/resume` + `/regenerate` acquire an in-memory per-conversation lock pre-flush (BEFORE the user-row insert / TX1 delete) — a second turn racing a live one gets **409 `turn_in_progress`** instead of interleaving rows. TTL 5 min: a leaked/zombie lock is aborted and taken over. In-memory like rate-limit.ts (Redis swap path for multi-instance). The web client maps the 409 to a toast, removes the optimistic rows and RESTORES the text into the composer.
  - **Turn abort on disconnect:** the lock's `AbortController` is wired to the request signal AND the SSE `ReadableStream.cancel()` — a refresh/Stop/tab-close actually STOPS the server loop (`runAgentTurn` checks the signal between steps; `chatStreamAgentic` fetch gets it). An aborted turn persists NOTHING (same trailing-user recovery as before); previously the abandoned loop ran to completion as a zombie writer — the root of the observed history-corruption incident (refresh mid-stream → regenerate → two live turns → gateway 400 "No tool output found" → generic error).
  - **Deterministic transcript order:** Postgres `now()` is FIXED per transaction, so DEFAULT-stamped rows of one `persistTranscript` commit all TIED on `created_at` and reload/replay order was arbitrary. `persistTranscript` now stamps explicit `base+i` millisecond increments; all message reads order by `created_at, id` (deterministic for legacy tied rows). **Clock-skew anchor:** `base = max(Date.now(), conversation MAX(created_at)+1)` — the Docker-VM Postgres clock drifts from the host's (notably after host sleep), and a raw `Date.now()` base intermittently sorted a turn's rows BEFORE/AFTER the wrong neighbours (a real flake caught at 7 failures/run). EVERY direct message insert is explicitly stamped via `nextMessageStamp()` (the /stream user row + all three /resume `role:tool` inserts) — never rely on the DB default `now()` next to host-stamped rows.
  - **Replay sanitizer:** `reconstructHistory` generalizes the old trailing-row dangling guard into a two-pass whole-history sanitation — a tool call counts answered only when its `role:tool` row follows its assistant row; unanswered calls are stripped (a row left with zero calls is dropped) and orphan/duplicate tool rows are skipped. The gateway can no longer 400 on a corrupted history.
  - **create_card robustness (the `note_type_not_found` bug):** the note type resolves LIVE — `BASIC_NOTE_TYPE.id` literals are NOT trusted (long-lived DBs keep legacy builtin rows under different ids; `ON CONFLICT DO NOTHING` preserved them). `noteTypeId` accepts a UUID or a case-insensitive NAME; field keys map case-insensitively onto the note type's real fields ("front"→"Front"); deck/note-type errors list valid options (self-correcting); UUID-shaped args are validated BEFORE reaching a Postgres `=` (no opaque 22P02).
  - **Validate-before-pause:** write/SRS tools expose optional `validate(ctx,args)` (read-only); an invalid proposal does NOT pause for confirmation — the error returns to the model as a fully-answered tool-call row and the loop continues to self-correct. Tests: `agent-robustness.test.ts` (legacy-id builtins, name/case normalization, no-pause-on-invalid, 409 lock, sanitizer, monotonic timestamps).
  - **Attachments (images + text files):** migration `0015`-era column `messages.attachments` jsonb (additive, `0014_goofy_triathlon.sql`). Stream body `attachments?` (max 4): `{kind:'image', mediaId}` refs an ALREADY-uploaded media object (web reuses `store.uploadMedia` presign→POST→finalize; the server resolves token/mime from the user-scoped `media` row — never trusts the client; foreign/unverified dropped) and `{kind:'text', name, text}` rides inline (re-capped 16k). The STORED user `content` stays clean — the model sees text files as `<attached_file>` blocks and images as multimodal `image_url` parts with base64 data URLs (`apps/api/src/ai/attachments.ts`: `getObjectBytes` from S3, last 4 images across the replayed history, ≤4 MB each; older/failed → `[attached image: …]` placeholder). `CHAT_VISION='false'` kill-switch for non-multimodal gateways (hides the attach-image affordance via `/ai/status.visionEnabled`; text files unaffected). Attachment-only sends are valid (`content` may be empty when attachments present; empty+none → 400 `empty_message`). Composer: paperclip button + paste-screenshot + drag-n-drop → chips (image thumbnails); user bubbles render image previews (`/m/<uuid>` via the Next rewrite) + file chips. Test seam `__setImageLoaderForTests` (no S3 in tests).
  - **Reload reconstruction (web):** `reconstructMessages` MERGES all assistant rows of one turn (tool_calls rows + the final prose row) into ONE MessageVM — the same shape the live stream builds — instead of N stacked "Assistant" blocks; `role:tool` rows match across the whole transcript (robust to legacy tied-timestamp shuffles); per-row `usage` stamps SUM into turn totals. An undecided pending write renders an amber "awaiting confirmation" badge (pause icon), never a fake spinner.
  - **Prompt:** `buildAgentSystemPrompt` gained a write-workflow section (list_decks-before-create, one-card-at-a-time proposals, real-id requirements, READ-the-tool-error-and-retry) and an attachments explainer line.
- **NotebookLM-style sources → linked cards (M1 — data + ingest + library):** the start of "load a book/PDF/URL, read it, chat grounded on it, generate flashcards that remember where they came from". M1 ships the DATA + INGEST + LIBRARY layer; the grounded-chat workspace + reader (M2) and the provenance backlinks (M3) build on the schema laid down here. Migration `0015_closed_jimmy_woo.sql`.
  - **Entities (`packages/db/src/schema/app.ts`):** `notebooks` (a collection of sources, user-scoped) → `sources` (one ingested PDF/EPUB/URL/text; the ROW IS THE INGEST JOB) → `source_chunks` (SoT for parsed document text) + `card_sources` (provenance edge, schema-only in M1, populated in M3). Embeddings reuse the existing `kb_chunk` table via its **AC7 seam** (`source_type='document'`, `source_id=source.id`, `parent_id=source.id` since the Library refactor — was `notebook.id` in M1, `card_id=NULL` — the existing CHECK already permits this) + a new partial unique `kb_chunk_source_pos_uq (source_id, position) WHERE source_type='document'` for the document upsert.
  - **Documents are PRIMARY SoT, NOT a disposable index.** The "kb_chunk is rebuildable from cards.render_text" invariant holds ONLY for `source_type='card'`. Document chunk text lives in `source_chunks` (the SoT) and SURVIVES a kb_chunk rebuild; `kb_chunk.text` for document rows is a DENORMALIZED read-cache copy of `source_chunks.text` (kept in sync — the ingest worker writes both, and document-reconcile rewrites both). `kb_chunk.source_id` is a plain uuid with NO FK → deleting a source needs an EXPLICIT `DELETE FROM kb_chunk WHERE source_type='document' AND source_id=…`. **Superseded by the Library refactor (M-Library, see below):** sources are user-level now, so `kb_chunk.parent_id` for document rows = `source_id` (NOT the notebook), and the old notebook-delete `… AND parent_id=<notebookId>` cleanup is GONE (a shared source's vectors must outlive any one notebook). Source-delete cleanup by `source_id` is the only kb_chunk doc cleanup.
  - **Card-graph guard (mixed corpus):** `kb_chunk` now holds both card and document vectors in one global HNSW index. `retrieve.ts`, `semantic-edges.ts` (incl. the LATERAL probe), and `similar.ts` (via retrieve) carry an explicit `source_type='card'` SQL conjunct BEFORE the LIMIT — NOT to prevent leakage (3-valued logic already drops `card_id IS NULL` rows) but to stop document vectors from STARVING the card top-k (`< k` card neighbours). Pinned by a COUNT-PRESERVATION test (`kb-chunk-document-guard.test.ts`), not a visibility test.
  - **Ingest pipeline (`apps/api/src/ai/source-parsers.ts` + `source-ingest.ts`):** parsers are server-only + LAZY-imported (`unpdf` per-page PDF, `fflate`-unzip + `htmlToPage` reuse for EPUB chapters, `page-reader` reuse for URL, inline for text) — deps in `apps/api` ONLY (never web/shared, `transpilePackages` would leak them). EPUB has a decompressed-size cap (`MAX_SOURCE_DECOMPRESSED_BYTES`, default 200 MB — zip-bomb guard). The async worker (the `sources` row IS the job) runs with NO user session (pulls `user_id` from the row), uses CAS status transitions (`pending→parsing→indexing→ready|error|deleting`) + `FOR UPDATE SKIP LOCKED` claim, batches `embed()` reusing `EMBED_BATCH`, and is crash-safe: progress is COMPUTED (`COUNT(source_chunks WHERE embedded=true)`, never a stored counter), the `embedded` flag flips in the SAME tx as the kb_chunk write, resume embeds only `embedded=false`, and `resumeSourceIngestOnStartup()` re-claims non-terminal rows on boot (wired in `index.ts` next to `reconcileOnStartup` + a shutdown drain). Idempotent upserts (`source_chunks` on (source_id,position); kb_chunk on the partial unique). Delete-during-ingest is closed by soft-delete `status='deleting'` + an in-tx `status<>'deleting'` re-check before each batch write. `notebooksEnabled` off (no embed key) ⇒ parse-and-park (SoT written, embedding deferred). Degraded dim ⇒ park, don't churn.
  - **Routes (`apps/api/src/modules/notebooks.ts`):** notebook + source CRUD (user-scoped, 404 foreign), aggregate caps (`MAX_NOTEBOOKS_PER_USER` / `MAX_SOURCES_PER_NOTEBOOK` → 409), claim-on-presign for pdf/epub (verified=false row claims the uuid under the caller; finalize SELECTs by `(id,userId)`), finalize byte-dedup (sha256 → 409 `duplicate_source` — **per-USER since the Library refactor**, was per-(user,notebook) in M1; returns `existingSourceId`), `GET /sources/:id` (status + computed `indexed`/`total` + machine `errorCode`), soft-delete + explicit kb_chunk cleanup. The source lifecycle (create/presign/finalize/delete/attach/reingest) now lives in `apps/api/src/modules/sources-shared.ts`, shared with the `/library` module. `/ai/status` reports `notebooksEnabled` (= `embeddingEnabled`; dim-degrade-aware).
  - **Env (all optional, `env.ai`):** `MAX_SOURCE_BYTES` (25 MB), `MAX_SOURCE_DECOMPRESSED_BYTES` (200 MB), `MAX_SOURCE_CHUNKS` (1500), `MAX_SOURCES_PER_NOTEBOOK` (50), `MAX_NOTEBOOKS_PER_USER` (25), `SOURCE_INGEST_CONCURRENCY` (1), `SOURCE_CHUNK_TOKENS` (800), `SOURCE_CHUNK_OVERLAP` (0.12). Error codes (`@neuronexus/shared` `INGEST_ERROR_CODES`) are machine codes mapped to `notebooks.status.*` i18n on the client — never English prose in the DB.
  - **Web (`apps/web/src/components/screens/notebooks.tsx`, route `/notebooks` + `/notebooks/[id]`):** the notebooks list screen — notebook CRUD, add-source (file picker → claim-presign→POST→finalize mirroring `store.uploadMedia`; url/text inline), per-source status badge polling via the extracted `use-source-status.ts` hook while non-terminal, `errorCode`→i18n, setup-notice when `!notebooksEnabled`. Nav entry in the sidebar LEARNING section (NOT bottom tabs). **Superseded by M-Library:** material storage + reading moved to `/library`; the notebook workspace is now `sources │ chat` + a citation-viewer (the full PDF reader lives at `/library/[id]` — see the Library section).
  - **Tests:** `notebooks` / `source-ingest` (status machine, computed progress, crash-resume no-double-embed, CAS idempotency, over-limit, parse-and-park) / `source-parsers` / `source-delete-race` (no orphan kb_chunk) / `source-dedup` / `source-text-parity` / `kb-chunk-document-guard` (count-preservation) + the shared document-chunker unit. Test seams: `__setPdfExtractorForTests` / `__setEpubReaderForTests` (parsers), `__setAiClientForTests` (embedder).
- **NotebookLM workspace (M2 — reader + grounded chat + citations; M3 — auto-provenance + backlinks):** the full loop on top of M1: read the source in-app, chat grounded STRICTLY on the notebook's sources, generate cards that remember which passages they came from. Migration `0016_brief_carmella_unuscione.sql` (additive: `conversations.notebook_id` + `messages.grounding`).
  - **Notebook-bound conversations:** `conversations.notebook_id` (uuid NULL, FK → notebooks **CASCADE** — a notebook's threads die with it; `card_sources.conversation_id/message_id` go SET NULL via their own FKs so created cards SURVIVE). `POST /chat/conversations` takes `notebookId?` (ownership 404); `GET /chat/conversations` WITHOUT params now filters `notebook_id IS NULL` (the global thread rail never shows notebook threads), `?notebookId=` lists that notebook's threads. **The grounding scope derives from the CONVERSATION row server-side — never from a request body** (spoof-resistant); the body only carries `sourceIds?` (per-turn checkbox scope, INTERSECTED with the notebook's `status='ready'` sources — foreign ids silently dropped; absent ⇒ all ready sources; `[]` ⇒ empty scope). **Library refactor:** the notebook's source set comes from a JOIN on `notebook_sources` (was `WHERE sources.notebook_id=…` in M2; `resolveNotebookScope` rewritten) — a shared source therefore drops out of chat the instant it leaves `ready` (e.g. during reingest) without touching the join edges.
  - **Source tools + registry:** notebook-mode `buildToolRegistry({ notebook: true })` = `search_source` (embed query → `retrieveDocuments` in `apps/api/src/ai/retrieve-documents.ts`: pgvector top-k over `source_type='document'` scoped `kc.source_id IN (sourceIds)`, user_id first conjunct, JOIN source_chunks/sources for sourceChunkId/page/heading/title; `sourceIds=[]` ⇒ `[]` без запроса) + `read_source` (sequential reading: `READ_SOURCE_CHUNKS` (3) chunks per slice from a `position`, continuation header) + `list_decks` + `create_card` (+ `web_search` when enabled, prompt-gated to explicit user requests — answers get "outside your sources:" labels). No search_cards/browse/progress/fetch_page/edit/SRS in notebook mode (V1). `buildAgentSystemPrompt({ notebook: { title, sourceTitles } })` dispatches to the source-grounded prompt variant; the write-workflow + user_instructions blocks are FACTORED fragments shared with the card variant (no fork-and-drift).
  - **Source citations:** `Citation` is now a union — `CardCitation` (legacy persisted rows lack `kind` ⇒ card) | `SourceCitation { kind:'source', sourceId, sourceChunkId, notebookId?, position?, page?, sourceTitle?, snippet? }` (`isSourceCitation` narrows). The model cites `[src:<sourceChunkId>]` (`SRC_TOKEN_RE`); the loop dedups + token-intersects PER VARIANT (cards by cardId, sources by sourceChunkId; fallback to capped union as before). The web chip opens the workspace reader scrolled to the chunk.
  - **Auto-provenance (server-written, AC3.1):** tools push distinct sourceChunkIds into the per-turn `ToolContext.grounding` accumulator (cap `GROUNDING_CAP` 24, exported from tools.ts). A SUSPENDED notebook `create_card` stamps the snapshot on its pending assistant tool_calls row (`messages.grounding` jsonb `{chunkIds}`) — provenance survives /resume + reload — and the `await_confirmation` impact carries a `provenance` preview (`{sourceTitle, page?, chunkId}[]`, cap `CARD_SOURCE_LINK_CAP` 5). On resume-APPLY, `writeCardProvenance` (`apps/api/src/ai/provenance.ts`) runs INSIDE the same tx as execute + the role:tool insert (atomic with the double-apply guard): resolves the PERSISTED grounding chunk ids (user-scoped, foreign dropped, cap `CARD_SOURCE_LINK_CAP`) → `card_sources` rows (cardId × chunk: sourceChunkId/sourceId/notebookId/conversationId/messageId = the pending row's id), `onConflictDoNothing({ target, where })` matching the PARTIAL unique (`targetWhere` does not exist for DoNothing — real drizzle 0.41 gotcha). Reject / all-excluded ⇒ no edges; a global (non-notebook) create_card writes none.
  - **Backlinks (AC3.4/3.5):** `GET /cards/:id/sources` (in cards.ts; LEFT JOINs — an all-NULL refs row is a TOMBSTONE rendered «источник удалён») and `GET /sources/:id/cards` (distinct cards + deckName + front excerpt) — both ownership-404, `{items:[]}` when empty. Reader pagination: `GET /sources/:id/chunks?from=&limit=` → `{ items, total, nextFrom }` (env `SOURCE_CHUNKS_PAGE` 50, max 200).
  - **Web:** `screens/chat.tsx` is now a thin shell over the EXTRACTED `components/chat/chat-panel.tsx` (`ChatPanel({ mode: 'global'|'notebook', notebookId?, sourceIds?, activeThreadId?, onThreadChange?, onSourceCitation? })`) — global mode is behavior-identical (same i18n/localStorage keys incl. `nn:chat:draft:*`, queue, confirm wizard, stop/regenerate, mentions, attachments, `?thread=`); notebook mode hides deck-scope/research/suggestions/`/research`, rides `sourceIds` on every stream/resume/regenerate and creates conversations with `notebookId`. **`/notebooks/[id]` is now `sources │ chat` (M-Library Р6) — was a 3-panel reader+chat in M2:** the sources rail (per-source «в чате» checkboxes persisted at `nn:nb:scope:<id>` + «N карточек» backlink counts + «Подключить из библиотеки» / detach) and the ChatPanel; a `[src:]` click opens the **citation-viewer** (text-mode chunk panel, scroll-to-chunk + flash + «Открыть в библиотеке» deep link) instead of an inline PDF. Full reading (PDF/ink/marks/quick-card) lives at `/library/[id]`. Card-side backlinks: `components/source-links.tsx` (`SourceLinksPanel`, module cache like similar-cards) mounted in the cards-browser edit dock + reviewer drawer (after reveal); renders null when the card has no provenance; tombstone rows for deleted sources; click jumps into the **library reader** (`/library/<sourceId>?page=&chunk=&mark=`).
  - **Env (M2/M3, all optional):** `CARD_SOURCE_LINK_CAP` (5), `SOURCE_CHUNKS_PAGE` (50), `READ_SOURCE_CHUNKS` (3).
  - **Tests:** `notebook-chat` (binding/filter, grounding frames, `[src:]` intersect, scope intersect, registry shape, prompt variants, chunks paging) / `card-provenance` (grounding stamp, edges incl. cap/reject/partial-exclusion/double-apply) / `source-backlinks` (routes, tombstones, cascade asymmetry) — 32 tests on the scripted fake-client pattern.
- **NotebookLM M4 — native PDF reader + Apple Pencil ink + AI-visible markup:** the reader for `kind='pdf'` sources renders the ORIGINAL PDF client-side (pdf.js) with a per-page ink overlay; the user's markup is persisted server-side AND readable by the chat agent. (M-Library moved this reader from the notebook workspace to `/library/[id]`.) Migration `0017_faulty_vampiro.sql` (additive `source_annotations`).
  - **Data:** `source_annotations(id, user_id↓, source_id↓cascade, page int 1-BASED (pdf.js convention), strokes jsonb, marked_text?, ts)`, plain unique `(source_id, page)`. Stroke model (`packages/shared/src/annotations.ts`): `PageAnnotations { v:1, strokes: InkStroke[] }`, `InkStroke { tool: 'pen'|'highlighter', color #rrggbb, width, points: flat [x,y,p,…] }` — coordinates NORMALIZED 0..1 (y down), so strokes are zoom/devicePixelRatio-invariant vectors, redrawn at any scale. Caps: `ANNOTATION_MAX_STROKES` 500 / `ANNOTATION_MAX_POINTS` 20000 (the BINDING DoS bound — the `SOURCE_ANNOTATION_MAX_BYTES` 512 KiB byte cap is structurally dormant under defaults: 20k points ≈ 390 KB) / `MARKED_TEXT_MAX` 8000.
  - **Routes (sourcesModule, all user-scoped 404-foreign):** `GET /sources/:id/file` (streams the original bytes from S3 by the row's `storageKey` — never a client-supplied key; url/text kinds → 404; S3 errors degrade to 404), `GET /sources/:id/annotations` (`{items:[{page, strokes, markedText, updatedAt}]}`), `PUT /sources/:id/annotations/:page` (structural `validateStrokes` → 400 `invalid_annotation`; byte cap → 400 `annotation_too_large`; EMPTY strokes ⇒ row DELETE → `{ok, cleared}`; else upsert `onConflictDoUpdate` on the PLAIN (source_id,page) unique — no `where` needed, unlike card_sources' partial index).
  - **AI sees the markup:** at save time the CLIENT extracts the text under the strokes geometrically — pdf.js `getTextContent()` item bboxes ∩ inflated stroke bboxes (pure math in `apps/web/src/lib/pdf-ink.ts`: transforms, y-flip, reading order, eraser hit-test — unit-tested, no DOM) — and sends it as `markedText`. The notebook-mode tool `list_marked_passages({sourceId?})` returns those passages per source/page AND resolves page-matched `source_chunks` into the grounding accumulator + `SourceCitation[]` — so «сделай карточки по тому, что я выделил» grounds, cites `[src:]`, and auto-links provenance. Scanned PDFs without a text layer degrade to empty markedText (strokes still persist). The prompt routes markup-references to the tool and names it in the untrusted-data line.
  - **Reader (`apps/web/src/components/pdf-reader/`):** `pdf-reader.tsx` — pdf.js is loaded as **NATIVE browser ESM, deliberately BYPASSING webpack**: `apps/web/scripts/copy-pdfjs.mjs` (wired directly into the web `dev`/`build` commands, NOT a lifecycle hook) copies the **LEGACY build** (`legacy/build/pdf.min.mjs` + `pdf.worker.min.mjs`) into gitignored `public/vendor/pdfjs/`, and the reader does `import(/* webpackIgnore: true */ '/vendor/pdfjs/pdf.min.mjs')` + `workerSrc = '/vendor/pdfjs/pdf.worker.min.mjs'`. TWO hard-won reasons, do NOT "simplify" back: (1) Next 16's bundled webpack mis-compiles pdfjs-dist's ESM at module init — "Object.defineProperty called on non-object" (webpack#20095, fixed only in webpack 5.103 which Next doesn't ship; pdf.js#20478; reproduced here on EVERY pdfjs 5.4.x incl. 5.4.394 and the legacy build); (2) the MODERN build assumes a bleeding-edge JS engine (5.7 worker dies with "a.toHex is not a function" — `Uint8Array.toHex` — on slightly-older engines incl. iPad Safari), the legacy build ships polyfills with an identical API. CSP already covers both (`script-src 'self'` for the same-origin module import, `worker-src 'self' blob:`). The reader load `catch` logs `[pdf-reader] load failed` + the cause to the console (the UI error state is generic). Continuous vertical pages with IntersectionObserver VIRTUALIZATION (±2 pages, canvases freed + render tasks cancelled on eviction), fit-width default, zoom 0.5–4 (toolbar + two-finger pinch: live CSS transform → re-render on settle), page indicator/jump, reading position `nn:pdf:pos:<sourceId>`, byte-progress load. `ink-layer.tsx` — dpr canvas per page; PALM REJECTION: `pointerType 'pen'`/'mouse' draw, 'touch' only with the finger-draw toggle (a second concurrent pointer never hijacks an in-flight stroke); pressure-modulated pen via `getCoalescedEvents`; highlighter = alpha 0.35 + 'multiply'; stroke-level eraser (normalized-space hit-test — inherently zoom-independent); per-page undo/redo (⌘Z); debounced ~800 ms PUT with save-status dot. Toolbar: hand/pen/highlighter/eraser, 6 colors, 3 widths, finger-draw toggle, zoom, page jump, «PDF | Текст» mode toggle (mode persisted `nn:nb:readermode:<sourceId>`, default 'pdf' for pdf kind; tools persisted `nn:pdf:tools`). Citation jumps use `citation.page` in PDF mode (no page → falls back to the text-mode chunk jump); backlink deep links carry `&page=`.
  - **Env (optional):** `SOURCE_ANNOTATION_MAX_BYTES` (512 KiB). pdfjs-dist@^5 lives in apps/web ONLY and ONLY as the source for the vendor copy + TS types — it is never imported through the bundler (see Reader above); v6 needs node ≥22.13.
  - **Tests:** `source-annotations` (validation matrix, upsert/clear, re-cap, foreign-404 incl. «no rows created under attacker», file-route 404 paths — NO S3 seam exists, byte-roundtrip untested by design) / `marked-passages` (registry shape, per-row caps, scope errors, END-TO-END: marked page → page-matched chunks → grounding stamp → apply → card_sources edges) / web `pdf-ink` unit suite.
- **NotebookLM M5 — reading-first workflow + production polish:** work with the document WHILE reading — select text in the PDF → floating popover (highlight colors · place-anchored note · quick card · ask chat · copy). Migration `0018_rapid_maddog.sql` (additive `source_marks`).
  - **Data:** `source_marks(id, user_id↓, source_id↓cascade, page 1-based, kind 'highlight'|'note', quote ≤MARK_QUOTE_MAX, rects jsonb MarkRect[] (normalized 0..1 selection quads — zoom-invariant render anchor), color ∈ SOURCE_MARK_COLORS, note ≤MARK_NOTE_MAX, ts)`. CRUD on sourcesModule (`GET/POST /sources/:id/marks`, `PATCH/DELETE /sources/:id/marks/:markId`), validation matrix → 400 `invalid_mark`, per-source cap 2000 → 409 `too_many_marks`.
  - **Quick card with provenance:** `POST /sources/:id/quick-card {deckId, front, back, page?}` — resolves builtin Basic LIVE (exported `resolveNoteTypeForCreate`), note+cards+`card_sources` edges in ONE tx (page-matched chunks cap `CARD_SOURCE_LINK_CAP`; no chunk match ⇒ one fallback edge with NULL sourceChunkId — check-then-insert in-tx), RAG enqueue post-commit. `POST /sources/:id/suggest-card {quote, page?}` → `{front, back}` via the non-streaming `complete()` (`ai/suggest-card.ts`: excerpt wrapped in `<excerpt>` delimiters as DATA-not-instructions, defensive JSON parsing, caps 1000; 503 `ai_disabled` / 502 `suggest_failed` — never 500).
  - **AI sees ALL markup:** `list_marked_passages` merges THREE sources per (source,page) in page order — ink `marked_text` + highlight quotes («[выделение]») + notes («[заметка]: quote — note») — same page-matched chunk grounding ⇒ «сделай карточки по выделенному» covers text highlights and notes too.
  - **Reader workflow (web):** pdf.js TextLayer per visible page (lazy, freed with virtualization; container `pointer-events:none` ALWAYS, glyph spans get `pointer-events:auto` only in hand mode via `.nn-textlayer[data-hand="1"] span` — so note-pin/highlight clicks land in the gaps). **Text-layer geometry is the OFFICIAL pdf.js CSS contract, do NOT hand-roll/simplify:** the vendored TextLayer sets per-span `--font-height` + `left/top` in % and our JS sets `--total-scale-factor` (NOT `--scale-factor` — the v5.7 bundle reads the former); the span FONT SIZE comes from the `font-size: calc(var(--text-scale-factor) * var(--font-height))` rule chain (`--min-font-size`/`--min-font-size-inv` + `transform: rotate(--rotate) scaleX(--scale-x) scale(--min-font-size-inv)`) copied from the vendored `pdf_viewer.css` into `.nn-textlayer` (globals.css). A hand-rolled subset renders every line compressed/offset («кривое выделение»). The pdf.js-required `user-select:none` on `span[role='img']`/`.endOfContent` is allow-listed in `native-feel-css.test.ts` (PM-2 invariant). Marks created while the layer was misaligned carry wrong rects — delete and recreate; selection → normalized merged rects (pure helpers in pdf-ink.ts) → `SelectionPopover` (flips below near the top edge); highlights layer (z1, multiply) + 📝 margin pins; «Разметка» panel (toolbar toggle with count badge — page-grouped highlights/notes/ink with jump+flash, «В карточку» from an existing mark); `QuickCardDialog` (deck remembered `nn:nb:quickdeck`, «✨ Сформулировать» hidden when chat off, inline create-error line, ⌘Enter); «Спросить» prefills the chat composer via the ADDITIVE `ChatPanel.composerPrefillRef` imperative handle (`{prefill(text)}` — global /chat unaffected); deep links `?mark=<id>` (+ existing `?page=`). Text-mode reader: popover with card/ask/copy only.
  - **Visual system (designer pass, globals.css):** `.nn-pdf-page` (elevated white sheets), `.nn-reader-bg`, `.nn-reader-toolbar` (frosted single row, 36px `.nn-tb-btn`, `.nn-tb-sep` groups, pulsing save dot), `.nn-source-row` (lime left-bar selected state, hover-revealed actions), `.nn-scroll` themed thin scrollbars, `.nn-empty-state` unified empty pattern, `.nn-ws-tab` mobile tabs, `.nn-dialog-backdrop` — all with `[data-theme="light"]` overrides + `prefers-reduced-motion` guards.
  - **Tests:** `source-marks` (25: validation matrix, caps, re-caps, ordering, PATCH/DELETE, foreign-404 + zero attacker rows) / `quick-card` (14: provenance edge variants incl. NULL-chunk fallback, cap, foreign deck/source, suggest-card off/garbage/throw) / `marked-passages` +1 (three-way merge in page order + highlight-page provenance through create_card apply).
- **Library (M-Library / L1–L4) — материалы first, ноутбуки second:** the two-level Zotero+NotebookLM model. A "library item" IS a `sources` row (the table is NOT renamed — Р1); **sources are USER-LEVEL now**, notebooks attach via a many-to-many join, so one book = one ingest + one set of embeddings no matter how many notebooks use it, and it survives any notebook's deletion. Reading (PDF/ink/marks/quick-card) moved to a standalone `/library/[id]` reader; the notebook is a cheap, disposable "research under a question". Migration `0020_known_klaw.sql`.
  - **Ownership model (the core refactor):** new join `notebook_sources (user_id↓, notebook_id↓cascade, source_id↓cascade, UNIQUE(notebook_id, source_id))`; `sources.notebook_id` + `source_chunks.notebook_id` are DROPPED (backfilled into the join in-migration). `kb_chunk.parent_id` for document rows = `source_id` (kept NOT NULL — a naïve `SET NULL` would roll back the whole transactional migration; §3.4). Cascades give Р3: delete a notebook ⇒ only its join rows + `conversations` die (sources/chunks/vectors/markup untouched); delete a source ⇒ its join rows + chunks/marks/annotations cascade, `card_sources` SET NULL (tombstones), explicit kb_chunk doc cleanup. New `sources` metadata columns (additive, nullable): `author`/`description`/`language`/`page_count`/`cover_media_id`→media SET NULL/`tags text[]`+GIN. New `source_reading_state (source_id,user_id unique, status 'unread'|'reading'|'finished', page, chunk_pos, percent)` — server-side reading progress (localStorage `nn:pdf:pos:*` is a write-through cache, migrated on first open).
  - **`/library` API (`apps/api/src/modules/library.ts`; lifecycle shared via `sources-shared.ts`):** `GET /library` (paginated list + filters `q/kind/tag/reading/shelf`, sorts `added/title/lastRead`, one-query aggregates `indexed/notebookCount/cardCount/readingStatus/percent` — no N+1) · `GET /library/search` (semantic search over ALL ready sources → `retrieveDocuments`, grouped by source, degrades `{groups:[],reason}` when embeddings off/empty) · `POST /library/items` (inline url/text + optional `notebookId` attach) · `POST /library/items/presign` → `/finalize` (pdf/epub, per-USER byte-dedup → 409 `duplicate_source`+`existingSourceId`) · `GET /library/items/:id` (metadata + status + readingState + `notebooks[]` + cardCount) · `PATCH /library/items/:id` (explicit-map: title/author/description/tags/readingStatus/language/pageCount/coverMediaId-ownership) · `DELETE` (full source delete) · `PUT /library/items/:id/reading-state` (upsert, unread→reading auto) · **`POST /library/items/:id/reingest`** (L4: 409 `not_terminal` if mid-flight, 400 `not_reingestable` for text (no external carrier), one tx = wipe `source_chunks` + document kb_chunk + CAS `ready|error→pending`, then kick the worker; grounding-safe by construction — the source drops out of every notebook's chat while it re-indexes). Notebook attach/detach in `notebooks.ts`: `POST /notebooks/:id/sources/attach {sourceIds}` (idempotent `ON CONFLICT DO NOTHING`, cap), `DELETE /notebooks/:id/sources/:sourceId` (detach ≠ delete). The legacy `/sources/:id/*` routes (file/chunks/annotations/marks/quick-card/cards) are unchanged (already source-scoped).
  - **Reader (`apps/web/src/components/screens/library-reader.tsx`, route `/library/[id]`):** the full M4/M5 workflow (PDF + ink + highlights + notes + quick-card + «Разметка» panel + selection popover) PLUS: a table of contents (PDF outline / text headings), server reading-state (5 s debounce + localStorage migration), the «Спросить» → notebook handoff (Р7, `sessionStorage` prefill + `composerPrefillRef`), lazy PDF cover/pageCount/author backfill (NULL-only). **L4 additions:** «Экспорт в Markdown» in the «Разметка» panel (pure `lib/markup-export.ts` builds page-grouped MD from already-loaded marks + ink markedText; `buildMarkupMarkdown`/`downloadMarkdown`, unit-tested) and a header «N карточек» badge → a cards drawer (`GET /sources/:id/cards` → row click `/cards?focus=<id>`). The list screen's details sheet has a «Переиндексировать» action (non-text + terminal status; confirm; polls status via `use-source-status`'s `isNonTerminal`). pdf.js loading is UNTOUCHED (native-ESM vendor build).
  - **Citation-viewer (in the notebook workspace):** a `[src:]` click opens a text-mode chunk panel keyed on `sourceChunkId` (scroll-to-chunk + flash + «Открыть в библиотеке» deep link with `?page=`/`?chunk=`) — the only "reader" inside a notebook. Cites of a DETACHED source still open (source-scoped, ownership-only — «nothing is lost»); a DELETED source 404s → tombstone.
  - **Web store:** `reingestLibraryItem(id)` (`POST .../reingest`), plus the existing library list/detail/patch/delete/reading-state/attach/detach methods. i18n namespace `library.*` (both locales) — incl. `library.reingest.*`, `library.reader.cards*`/`export*`, `notebooks.marks.export`.
  - **Env (M-Library, all optional):** `MAX_LIBRARY_ITEMS_PER_USER` (300), `MAX_ATTACH_BATCH` (20), `LIBRARY_PAGE` (60); `MAX_SOURCES_PER_NOTEBOOK` (50) is now the attach cap.
  - **Migration discipline (Р9):** 0020 is a VERSIONED migration with an in-migration backfill (`INSERT INTO notebook_sources SELECT … FROM sources` BEFORE the `DROP COLUMN`s + the `UPDATE kb_chunk SET parent_id = source_id WHERE source_type='document'`) — `db:push --force` CANNOT do the backfill and would silently drop the links. **`db:push` is forbidden on this milestone until 0020 is applied;** the dev cycle is `db:generate` → hand-edit the SQL (steps 2 & 7 — precedent 0007) → `bun run db:migrate:apply`. For a local dev DB you can also apply a single migration directly via `psql "$DATABASE_URL" -f packages/db/src/migrations/0020_known_klaw.sql`. Schema in `app.ts` is edited synchronously. Tests run on `db:push:test` (the test DB has no precious data, so the additive shape is fine).
  - **Tests:** `library` / `library-meta` (list/filters/aggregates, PATCH explicit-map + coverMediaId ownership, search) / `source-reingest` (L4: CAS guards — text 400, foreign 404, non-terminal 409, two-concurrent CAS; wipe + re-index to ready; document-reconcile re-embeds on model change incl. idempotency + parked-when-off) / `source-dedup` (per-user inversion) / `source-backlinks` (notebook-delete keeps the source). Web: `markup-export` unit suite, `library-handoff`. Test seams: `__setAiClientForTests` (embedder) + `__setPageReaderForTests` (url parser).
- **Notebooks 2.0 (N1–N4) — research slice on top of the library:** a notebook stops being «sources │ chat» and becomes a full NotebookLM-style workspace: editable notes, generated study artifacts (a «студия»), self-scored quizzes, card-coverage of its sources, a concept-map, and library recommendations. The library still owns reading/material storage (M-Library) — a notebook is a disposable «research under a question». Migration `0021_skinny_bushwacker.sql` (ADDITIVE: notebook metadata columns + three new tables).
  - **Data model (0021, additive):** `notebooks +=` `emoji`/`color`(deck palette)/`description`(≤500)/`pinned`/`archived` + the overview cache `overview text`/`suggested_questions jsonb`/`overview_fingerprint text` (Р6). `notebook_notes` (user-editable markdown `manual` or saved chat answer `answer`; `citations jsonb` snapshot + `message_id REFERENCES messages ON DELETE SET NULL` — the note SURVIVES regenerate/thread-delete as a snapshot, back-ref just nulls; `pinned`; idx `(notebook_id, pinned DESC, updated_at DESC)`). `notebook_artifacts` (a generated document; the ROW IS A JOB — `status pending→generating→ready|error`; `type`, `title`, `content_md`|`content_json`(quiz), `source_ids jsonb` scope snapshot, `error_code`, `model`; partial active-index `WHERE status IN ('pending','generating')`). `quiz_attempts` (`answers jsonb`, `correct`/`total`, `artifact_id ↓cascade`). Caskades: notebook-delete ⇒ notes + artifacts (+ attempts via artifact cascade) die WITH the conversations/edges; sources + cards LIVE. Quiz-artifact delete cascades its attempts (history is meaningless without it); cards born from «weak spots» survive (their provenance is `card_sources` on a `source_chunk`, not the artifact).
  - **Artifact engine (`apps/api/src/ai/artifacts.ts`):** `generateArtifact(id)` is an async worker (CAS `pending→generating`, no session — pulls `user_id` from the row) that builds an EVEN context sample (`evenSample` round-robin across the snapshot's sources, re-verifying each is still `ready`+owned — a reingested source drops out ⇒ `error('no_sources')`), wraps it in `<source_material>` anti-injection delimiters with per-chunk `[src:<chunkId>]` ids, ONE `complete()` per type (timeout `ARTIFACT_TIMEOUT_MS` 60s), post-processes via `applyArtifactCitations` ([src:]-intersect with the SAMPLED context only, Р5), then CAS `generating→ready` (or `→error` with a machine code: `ai_disabled|timeout|generation_failed|invalid_quiz`). Six types: `summary`/`study_guide`/`faq`/`timeline`/`glossary` (markdown) + `quiz` (structured). `reconcileArtifactsOnStartup()` flips stale `pending|generating` → `error('interrupted')` on boot (next to `resumeSourceIngestOnStartup`). One generation per notebook: a per-notebook `pg_advisory_xact_lock(hashtext(id))` makes the EXISTS(active)+INSERT atomic → 409 `generation_in_progress` (the partial active-index alone can't serialize two racing EXISTS probes). `__setAiClientForTests` fakes WITHOUT `complete` ⇒ deterministic `error('ai_disabled')`.
  - **[src:]-intersect extraction (`apps/api/src/ai/citations.ts`):** the inline token-intersect that used to live in `runAgentTurn` is now `intersectSourceTokens(text, citations)` (pure; card-tokens by `cardId`, source-tokens by `sourceChunkId`; empty ⇒ `[]`, the chat loop keeps its own capped-union fallback). The artifact generator reuses the sibling `applyArtifactCitations(text, context)` (white-lists ONLY the sampled context's chunk-ids, strips hallucinated tokens). One source of truth; the chat loop is behaviorally unchanged (pinned by `notebook-chat`).
  - **Overview + fingerprint (Р6):** `POST /notebooks/:id/overview` (sync, `NOTEBOOK_OVERVIEW_TIMEOUT_MS` 20s) → `generateNotebookOverview` → `complete()` returns defensive-parsed `{overview, questions[]}` cached on the notebook row. The staleness key is `computeOverviewFingerprint` = FNV-1a over the sorted READY sourceIds + each one's **chunkCount** — NOT `sources.updated_at` (cover/author backfills bump that → false invalidation). `GET /notebooks/:id` returns both the cached `overviewFingerprint` and a live `currentFingerprint`; a mismatch ⇒ the client shows «Обновить обзор». Auto-kick ONCE per workspace mount (ref-guard) when overview IS NULL + ready sources + chatEnabled; on failure NO auto-retry (anti-loop of paid calls).
  - **Quiz (Р8):** `content_json = {questions:[{id, kind:'mcq'|'tf'|'open', prompt, options?, answerIndex?, answerText?, explanation?, sourceChunkId?}]}`, structurally validated AT GENERATION (`parseQuiz` — answerIndex in bounds, no dup ids, `[src:]` white-listed; bad JSON ⇒ `error('invalid_quiz')`). `clampQuestionCount` (default `QUIZ_QUESTIONS_DEFAULT` 10, cap `QUIZ_QUESTIONS_MAX` 20). `POST …/attempts` SERVER-RESCORES mcq/tf from `content_json` (a forged `correct` is ignored — `scoreQuizAttempt`); `open` trusts ONLY the client's `{selfCorrect}` boolean (the human grades — the single exception); unknown questionId → 400 `invalid_attempt`. The player's «слабые места → карточки» just prefills the chat composer from the wrong answers (zero new backend — the agent does a `create_card` batch with provenance).
  - **Coverage (Р9, SQL-only):** `GET /notebooks/:id/coverage` → per attached source `{sourceId, title, totalChunks, coveredChunks, cardCount, pct}` + a notebook aggregate. `covered` = DISTINCT `card_sources.source_chunk_id` among the source's chunks, **JOINed to `cards`** (a tombstoned/deleted card never counts — LIVE cards only). Plus `gaps`: top-5 `source_chunks.heading` with the most UNcovered chunks, each prefilling a «карточки по разделу» chat prompt. No new schema. Renders WITHOUT a chat key (only the gap-prefill buttons gate on chatEnabled).
  - **Notebook agent tools (Р14):** the notebook-mode registry gains `list_notes` (read: id+title+kind+excerpt+pinned, cap 50) + `read_note` (read: full markdown by id, notebook-scoped ownership) → «сделай карточки по моим заметкам» works out of the box; and `save_note` (WRITE with a confirm-pause, validate-before-pause on the title/content caps) → «сохрани это в заметки». Confirm-preview is an EXPLICIT new `ConfirmImpact.proposedNote?: {title, contentExcerpt}` (excerpt ≤300) — web `confirm-diff.tsx`/`chat-activity.ts` render a flat note branch (NOT the `create_card` wizard). Resume-apply goes the GENERIC `ai.ts` path — all create_card-specific branches (provenance, cardSelections, applyCreateCardSelections) stay gated on `pending.name === 'create_card'` and are no-ops for save_note (pinned by a test). Note content does NOT enter `grounding` (notes ≠ sources — no chunk-id to cite; the agent just reads the text).
  - **Concept-map (Р10, `apps/api/src/ai/concept-map.ts`, vectors-only):** `GET /notebooks/:id/concept-map` → a SECTIONAL semantic graph (NOT semantic-edges' per-card aggregation). TWO-PHASE because `heading` lives in `source_chunks` while the embedding lives in document `kb_chunk` rows: (1) sections = `GROUP source_chunks BY (source_id, COALESCE(heading, '#'||floor(position/10)))` (NULL-heading bucketed every 10 chunks so structureless text doesn't collapse to a mega-node), capped to `CONCEPT_MAP_MAX_SECTIONS` (60) via an even round-robin sample across sources; (2) a document-guarded k-NN-join over the capped sections' first chunks (EVERY subquery carries `kc.user_id` + `kc.source_type='document'` + `kc.source_id IN (…)` — the global HNSW index mixes card+document vectors), each probed chunk mapped back to its section, undirected dedup `LEAST/GREATEST` + `MAX(score)`, intra-section pairs dropped, top-3 edges/node in JS. No document vectors ⇒ `{nodes:[],edges:[],reason:'not_indexed'}`. Rendered as a dependency-free SVG force graph (web: `lib/concept-map-layout.ts` — a seeded-init/120-tick repulsion+springs+centering sim computed ONCE, NOT animated, so reduced-motion is honoured for free; `components/notebook/concept-map.tsx` — node click → citation-viewer on the section's first chunk).
  - **Suggest-sources (Р11, `apps/api/src/ai/suggest-sources.ts`, vectors-only):** `GET /notebooks/:id/suggest-sources` → centroid = `AVG(embedding)` over a ≤256 sample of the notebook's ATTACHED document vectors → ANN probe over UNATTACHED `status='ready'` document vectors (same document-guard trio + `source_id NOT IN (attached)` + JOIN `sources` user-scoped), GROUP BY source, top-5 `{sourceId, title, kind, score}`. Surfaced as a «Рекомендуем» section atop the «Подключить из библиотеки» attach-picker (already-attached filtered out). No vectors / all attached ⇒ `{items:[], reason?}`.
  - **Workspace (Р12, `notebook-workspace.tsx`):** desktop ≥1100 = `sources (280) │ chat (flex) │ dock (340)`; the right dock has tabs «Обзор / Заметки / Студия» (state `nn:nb:dock:<id>`/`nn:nb:docktab:<id>`), the citation-viewer is an OVERLAY mode of the dock zone (`citationViewer ?? dockColumn`), collapse swaps the column for a thin expand-rail. Tablet 720–1100 = `sources │ chat` with the dock as a right-side SHEET (header button + a badge-dot when notes are non-empty). Mobile <720 = a 3-tab switcher (Источники / Чат / Блокнот) with the dock's own segment tabs. ChatPanel gains ADDITIVE notebook props `onSaveAnswer?({content, citations, messageId?})` + `suggestedQuestions?` (empty-state pills). Components in `components/notebook/`: `notes-panel`/`studio-panel`/`overview-panel`/`quiz-player`/`coverage-bars`/`concept-map`. Artifact polling uses a dedicated `use-artifact-status` (`isArtifactNonTerminal` — its `{pending,generating}` terminal set ≠ source-status; enums not mixed). Store: `listNotebookNotes`/`createNotebookNote`/`patchNotebookNote`/`deleteNotebookNote`, `listArtifacts`/`createArtifact`/`getArtifact`/`deleteArtifact`/`regenerateArtifact`, `submitQuizAttempt`/`listQuizAttempts`, `generateOverview`(POST)/`getCoverage`/`conceptMap`/`suggestSources`, `patchNotebook`. i18n namespace `notebooks.*` extended (`notes`/`studio`/`quiz`/`overview`/`coverage`/`map`/`attach`/`meta`) — BOTH locales.
  - **Gating (Р17):** studio/overview/quiz require `chatEnabled` (buttons hidden, setup-notice in-tab; the server STILL gates → 503 `ai_disabled`, never 500). Concept-map/recommendations degrade by vector presence (`reason`). Notes/metadata/coverage/list work ALWAYS (no AI).
  - **Env (all optional, defaults):** `MAX_NOTES_PER_NOTEBOOK` (500), `NOTE_CONTENT_MAX` (16_000), `NOTE_TITLE_MAX` (200), `MAX_ARTIFACTS_PER_NOTEBOOK` (50), `ARTIFACT_CONTEXT_CHUNKS` (60), `ARTIFACT_TIMEOUT_MS` (60_000), `QUIZ_QUESTIONS_DEFAULT` (10) / `QUIZ_QUESTIONS_MAX` (20), `NOTEBOOK_OVERVIEW_TIMEOUT_MS` (20_000), `CONCEPT_MAP_MAX_SECTIONS` (60). Cap overruns → 409 with a machine code (`too_many_notes`/`too_many_artifacts`).
  - **Migration discipline:** 0021 is FULLY ADDITIVE (new tables + nullable columns) — dev DB applied via `psql "$DATABASE_URL" -f packages/db/src/migrations/0021_skinny_bushwacker.sql` (no dev migration journal — `db:migrate:apply` forbidden on dev). Test DB uses the standard `db:push:test`. `app.ts` schema edited synchronously.
  - **Tests:** `notebook-meta` (9: PATCH explicit-map, pin/archive updatedAt-semantics, counts, archived filter) / `notebook-notes` (9: CRUD, caps, foreign-404 + zero attacker rows, message SET NULL survival, search/sort) / `notebook-artifacts` (23: CAS status machine, 409 concurrency, [src:]-intersect, even context sample, timeout→error, reconcile, degrade-without-key) / `notebook-overview` (11: defensive JSON parse, fingerprint staleness, 503) / `notebook-quiz` (19: schema validation, bad JSON→error, server rescoring, attempts history) / `notebook-coverage` (7: tombstone filtering, heading gaps, empty sources) / `notebook-note-tools` (9: notebook-mode registry shape, save_note confirm-flow + caps, read-tools foreign-404) / `notebook-concept-map` (11: sectional aggregation, document-guard conjuncts, not_indexed degrade, section cap) / `notebook-suggest-sources` (9: centroid scope — only unattached ready, foreign exclusion). Web: `concept-map-layout` (13: convergence/non-NaN, determinism, coincident-seed separation). The scripted fake-client pattern throughout (`__setAiClientForTests`); no live AI calls.
- **Reindex-on-model-change runbook:** changing `EMBEDDING_MODEL` to one with a different dimension requires: (1) update `EMBEDDING_MODEL` + `EMBEDDING_DIM` env; (2) edit the `kb_chunk.embedding` column dimension in `packages/db/src/schema/app.ts`; (3) `drizzle-kit generate` from `packages/db` → a new `00NN_*` migration that drops + re-adds the `vector(N)` column + rebuilds the HNSW index (a dimension change can't be done in place); (4) apply via `bun run db:migrate:apply`; (5) `POST /ai/reindex` (or run `backfill.ts`) — every chunk's `source_hash` includes `embeddingModel`, so all chunks are detected as stale and re-embedded. Same-dimension model swaps skip steps 2–4 (only env + reindex). **Document chunks are ALSO re-embedded now (L4 closed the M1 deferral): `reconcileDocumentsOnStartup()` in `source-ingest.ts` runs after the card pass in `reconcileOnStartup` AND is kicked by `POST /ai/reindex` — it walks each `ready` source, finds chunks whose SoT `source_hash` ≠ hash(text+model) OR whose `kb_chunk.embedding_model` differs, re-stamps them `embedded=false`, and re-embeds through the SAME `embedUnembeddedChunks` ingest helper (reading `source_chunks.text` — the SoT, so no re-parse/re-download). Parks (no-op) under `embeddingDegraded()`/disabled — never churns. So a dim change reindexes cards AND documents.** Per-source manual re-embed is also available via `POST /library/items/:id/reingest` (full re-parse).

## Product shape

**NeuroNexus** is a reimagined Anki (spaced-repetition flashcards) with a graph view, garden gamification, and AI affordances. The architecture is **server-of-truth over Bun + Elysia + Postgres**, with a Next.js/React 19 client. Auth via BetterAuth. No offline mode yet — every page requires a logged-in session; the client is a thin mirror of the server's state.

The app was first built client-only against Dexie, then migrated to the current server-first shape. Expect occasional leftover files or comments from the Dexie era; delete on sight if they're clearly orphaned.

## Monorepo layout

```
apps/web           # Next.js App Router, port 3001
apps/api           # Elysia server on Bun, port 3000
packages/db        # Drizzle schema + client + migrations
packages/auth      # BetterAuth config (shared between api and web)
packages/shared    # Pure-TS types, FSRS helpers (no DOM/Node deps)
docker-compose.yml # Postgres 17
.env               # root env file, read by api (--env-file) and web (symlink)
```

Workspaces are declared in the root `package.json`; cross-package deps use `"workspace:*"`. **Next.js needs `transpilePackages: ['@neuronexus/*']`** in `apps/web/next.config.ts` — it does not auto-transpile workspace TS.

## Backend architecture (apps/api)

### Entry point
`apps/api/src/index.ts` composes the server: `cors` → `swagger` → `authPlugin` → `/health` → `onError` → domain modules. **Do not `export default app`** — if you do, Bun auto-binds `Bun.serve(app)` and collides with the explicit `.listen()` call. Only `export type App = typeof app` for Eden Treaty.

### Auth plugin
`apps/api/src/auth-plugin.ts` both `.mount(auth.handler)` (exposing `/api/auth/*`) **and** defines a reusable `auth: true` macro that resolves `{ user, session }` or returns 401. Every protected route passes `{ auth: true }` in its route config — this is how the route gets typed access to `user.id`.

### Domain modules
Each under `apps/api/src/modules/*.ts`, prefixed with its resource (`/decks`, `/cards`, `/reviews`, `/profile`). All operations scope by `user.id` pulled from the session. FK `ON DELETE CASCADE` on `decks.parent_id`, `cards.deck_id`, `reviews.card_id` and `.deck_id` means deleting a deck cascades the full subtree at the DB level — do NOT re-implement the cascade in the handler.

### FSRS grading
`apps/api/src/modules/reviews.ts` reconstructs a `ts-fsrs` Card shape from the flat Drizzle columns, calls `gradeFsrs` from `@neuronexus/shared` with the user's `desiredRetention`, then writes back the new FSRS state + a `reviews` row + an updated `profile` (streak/xp/level/plantStage) inside a single `db.transaction`. Keep grade handling transactional — it's the only multi-table mutation in the app.

**Leech auto-suspend:** after a grade, if `card.lapses` crosses the threshold (Anki default: 8), `suspended` is set to `true` in the same transaction and the response includes `leeched: true`. Subsequent `POST /reviews` on a suspended card returns 409.

**XP formula:** `{ Again: 0, Hard: 5, Good: 10, Easy: 15 }` — defined in `xpForRating` in `@neuronexus/shared`. Formula lives there, not in the handler, so the web app can preview deltas client-side.

### Review queue
`GET /cards/queue?deckId=…&newLimit=20&reviewLimit=200` returns `{ due, new, total }`. Due list is ordered by `due ASC`, new list by `createdAt ASC`. Suspended cards are excluded from both. Call this — not `GET /cards?due=true` — when building the reviewer UI: that endpoint is for ad-hoc queries and lacks the daily caps.

## Database (packages/db)

- `schema/auth.ts` — BetterAuth tables (`user`, `session`, `account`, `verification`). Regenerate with `bun run --filter @neuronexus/auth auth:generate` if you add plugins.
- `schema/app.ts` — domain tables (`profile`, `decks`, `cards`, `reviews`). FSRS state flattened into columns; `due` indexed together with `user_id` for the "what's due now" query.
- `client.ts` — singleton `postgres()` pool + `drizzle()` instance. Use `db` + `schema` from `@neuronexus/db`.
- `drizzle.config.ts` — the `loadRootEnv()` function walks up from `packages/db` to parse the root `.env` manually, because drizzle-kit spawns a Node subprocess that doesn't inherit Bun's auto-loaded env. Don't replace this with `dotenv` — it's intentionally zero-dep.

**Schema changes workflow:** edit schema → `bun run db:push` in dev (destructive, no history). For prod, switch to `db:generate` + `db:migrate` and commit the generated SQL files.

## Auth (packages/auth)

Two entry points, imported separately to avoid pulling server code into the browser bundle:

- `@neuronexus/auth/server` — `auth` (BetterAuth instance with `drizzleAdapter`). Used by `apps/api` and, if needed, by Next.js Server Components via `auth.api.getSession({ headers })`.
- `@neuronexus/auth/client` (also `@neuronexus/auth` barrel) — `authClient` + `signIn`, `signUp`, `signOut`, `useSession`. Configured with `credentials: 'include'` to send the session cookie cross-origin in dev (api on :3000, web on :3001).

Email+password only right now. Sessions are HTTP cookies; on prod move both apps behind a single domain (or shared parent via `advanced.crossSubDomainCookies`).

## Frontend architecture (apps/web)

### Server-first data flow
- `src/lib/api.ts` — **Eden Treaty** client: `treaty<App>(baseURL, { fetch: { credentials: 'include' } })`. `credentials: 'include'` is mandatory — without it BetterAuth session cookies don't cross the :3000↔:3001 boundary. Also exports `ok(result)` which unwraps Eden's `{ data, error }` envelope and throws on non-2xx. **Every API call goes through this.**
- Eden's inferred types occasionally choke on the store's generic patch handlers; calls in `store.ts` cast through `(api as any)` intentionally. When adding new endpoints, start without the cast — only fall back to `any` if TS explodes on `treaty<App>` path chains.
- `src/lib/store.ts` — Zustand store. Each method translates to one API call; the store holds a cached mirror for synchronous reads in components. No more Dexie.
- `src/lib/mappers.ts` — converts server rows (ISO dates, column names like `learningSteps`) into the UI types in `lib/types.ts` (epoch numbers, camelCase, reconstructed FSRS Card). If you add a column, extend the mapper, not the component.
- `src/lib/bootstrap.tsx` — watches `useSession()`. When a user signs in, fetches `/profile /decks /cards` in parallel and populates the store. On sign-out, clears the mirror.

### Auth UX
- `apps/web/src/app/auth/sign-in` and `/sign-up` are the public routes (render under `app/auth/layout.tsx`, no app shell).
- `apps/web/src/components/auth-gate.tsx` wraps the `(app)` group. Client-side guard: if no session, `router.replace('/auth/sign-in?next=...')`. Client-only because the whole app is statically prerendered.
- After sign-in `useSession()` flips `data.session` truthy, bootstrap fires, app renders.

### Routing
- `apps/web/src/app/layout.tsx` — fonts, `I18nProvider`, `Bootstrap` (no auth gate here — sign-in pages live under this layout too).
- `apps/web/src/app/(app)/layout.tsx` — `AuthGate` → `AppShellWrapper` (sidebar + topbar + bottom tabs + global overlays).
- `apps/web/src/app/(app)/*/page.tsx` — route stubs, all `'use client'`, all render `<NNTopbar />` + a screen component from `src/components/screens/`.

### Styling
Design tokens are CSS custom properties in `apps/web/src/app/globals.css` (`--ink-*`, `--lime-*`, `--surface`, `--r-md`, `--shadow-lg`, `--font-sans`, …). Dark is default; `[data-theme="light"]` overrides. UI primitives in `apps/web/src/components/ui.tsx` — `NNIcon`, `NNBtn` (7 variants × 4 sizes), `NNBadge`, `NNCard`, `NNKbd`, `NNLogo`, `NNPlant`, `NNHeatmap`, `NNMiniGraph`, `NNTag`. Inline `style={{}}` referencing CSS vars; no Tailwind.

### i18n
`apps/web/src/lib/i18n.tsx` → `useT()`, `useLocale()`, `useDateLocale()`. Dictionaries at `lib/messages/{en,ru}/*.ts`. Add a string to **both** locales, reference via `t('<ns>.<key>')`. Dot notation with `{name}` interpolation.

### Responsive shell
Breakpoints in `lib/use-breakpoint.ts` — mobile <720, tablet 720–1100, desktop ≥1100. `components/app-shell.tsx` swaps inline sidebar ↔ drawer (opened via `window.dispatchEvent(new Event('nn:open-drawer'))`). Bottom tabs on mobile only. Drawer state is driven by custom window events `nn:open-drawer` / `nn:close-drawer` — **do not** prop-drill open handlers.

### Global overlays
`components/overlays/global-overlays.tsx` listens for `⌘K`, `?`, and custom events `nn:open-palette` / `nn:open-cheatsheet`. Any component can open an overlay via `window.dispatchEvent(...)`. `?` handler skips when focus is in an editable element.

### Empty-state redirects
`lib/use-empty-redirect.ts` — `useEmptyRedirect('first-run' | 'done' | 'graph')`. Waits for `bootstrapped` before redirecting so initial fetches don't flash the empty screen.

## Gamification (shared + reviews handler + /achievements)

The grade handler is the single place where gamification state advances. It calls `applyGradeRollup` from `@neuronexus/shared` inside the same transaction that writes the FSRS card + review row. Rollup semantics:

1. **Streak + freeze:** `applyStreakWithFreeze` bumps `streak_days` when yesterday=last-review, consumes a freeze when there's a **one-day** gap (multi-day gaps always reset), and resets otherwise. Capped at `MAX_STREAK_FREEZES` (5).
2. **Today minutes ledger:** `today_minutes` + `today_minutes_date` accumulate `durationMs` across grades within the same calendar day; reset when a new day arrives.
3. **Daily goal stamp:** once today's minutes cross `daily_goal_minutes`, `daily_goal_met_count` increments **once** per day.
4. **Achievement evaluator:** `evaluateAchievements(stats, alreadyUnlocked)` returns `{ code, def }[]`. Stats snapshot is a live DB read inside the transaction (`totalReviews` via `COUNT(*)`, `deckCount` via `COUNT(*)`), so "polyglot" (3 decks) fires on the first grade after the user creates the 3rd deck.
5. **Rewards:** achievement definitions can carry `{ streakFreezes, species[], xp }`. Rewards are summed via `sumRewards`, species are added to `profile.unlocked_species` (dedup'd), freezes and xp added onto the running totals.

The catalog lives in `packages/shared/src/gamification.ts` → `ACHIEVEMENTS` (16 codes today: streak 7/30/100/365, reviews 100/1000/10000, decks 3/10, level 5/10/20, garden 3/5, dailyGoal 7/30). Keep the catalog monotonic within each `kind` — the unit tests enforce that.

**Plant species unlock:** five species beyond the default `fern` (`cactus`, `succulent`, `bonsai`, `sakura`, `mushroom`). Each is gated by an achievement's `reward.species`. `profile.plant_species` still stores the *current* selection; `profile.unlocked_species` lists everything the user can choose.

`POST /reviews` response now carries `{ card, review, profile, leeched, newAchievements, freezeUsed, dailyGoalJustMet }` — the web layer uses these to trigger toasts / confetti / freeze indicators.

The read API is `GET /achievements` (per-user list with unlockedAt + progress + pct), `GET /achievements/summary` (home-banner count + 3 most recent), and `GET /achievements/catalog` (static catalog, client-cacheable).

## Shared FSRS (packages/shared)

`packages/shared/src/fsrs.ts` wraps `ts-fsrs` for both server (grading) and client (rating preview). Use `Rating 1|2|3|4`, mapped to FSRS `Grade` via `ratingMap` — do NOT use `FsrsRating` directly (it includes `Manual`).

`getScheduler({ requestRetention, enableFuzz, deterministicSeedCardId })` memoizes one `fsrs()` instance per unique cache key. This lets per-user `desiredRetention` work without allocating a scheduler on every call. In tests, pass `{ enableFuzz: false }` for exact-`due` assertions.

Anki-compatible defaults exported as `ANKI_DEFAULTS`: `requestRetention=0.9`, `learningSteps=['1m','10m']`, `relearningSteps=['10m']`, `leechThreshold=8`, `maximumInterval=36500` days. Keep this module the single source of truth for those numbers — the reviews handler imports them; don't inline magic constants.

Streak / XP / plant-stage helpers (`nextStreak`, `xpForRating`, `levelFromXp`, `plantStageFromStreak`) are here too, so both the grade handler and UI previews agree.

## Testing

- **Test runner:** `bun test` (built-in). Files: `*.test.ts` anywhere in the tree. `bun run test` at the root runs every suite, after running `bun run db:push:test` (the `pretest` hook) to keep the test DB schema current.
- **Test database:** `TEST_DATABASE_URL` points at `neuronexus_test`, created by `docker/postgres-init.sh` on first container boot. `packages/db/src/env.ts` switches to it automatically when `NODE_ENV=test`, so there's no way to accidentally hit the dev DB from tests.
- **Integration pattern:** tests import `buildApp()` from `apps/api/src/app.ts` and call `app.handle(new Request(...))` — no network, no port binding. Helpers in `apps/api/tests/helpers.ts` wrap body/cookie serialization (`callApp`), cookie extraction (`extractCookie`), and sign-up (`signUpAndCookie`). Every `beforeEach` calls `resetTestDb()` which `TRUNCATE … RESTART IDENTITY CASCADE`s the full domain + auth table set (refuses to run unless the URL contains "test").
- **FSRS tests use `{ enableFuzz: false }`** to make `due` assertions stable. If you need fuzz on in a specific test (e.g., testing the fuzz bounds), pass `deterministicSeedCardId` to pin the RNG.
- Current suite: **1319 tests passing** (unit + integration — auth, decks, cards, reviews, GDPR, rate-limit, note-types, undo, rich-content, AI/RAG, chat tools, stats, semantic similar/edges, agentic-environment pack: usage/parallel-tools/titling/pinned/instructions/mentions/compression/confirm-previews, agent robustness: turn-lock/replay-sanitizer/validate-before-pause/legacy-builtin-ids, chat attachments, batch create_card + live tool_result summaries, deep research: page-reader SSRF/Exa + agent fetch_page, chat-threads/chat-mentions web helpers). Run one file: `NODE_ENV=test bun --env-file=./.env test apps/api/tests/reviews.test.ts`.

## Builtin note types

Migration `packages/db/src/migrations/0007_builtin_note_types.sql` seeds the three system note types (Basic, Cloze, Type-in) on every fresh database via a partial unique index `note_types_builtin_uq` + `INSERT … ON CONFLICT DO NOTHING`. This replaced the old seed-only path that made note-type creation impossible on prod without a manual seed run.

`ensureBuiltins()` from `@neuronexus/db` (`packages/db/src/ensure-builtins.ts`) is the test/startup contract — call it in `beforeEach` helpers and at API startup to guarantee the three rows exist before any note-type-dependent operation. Stable UUID literals are exported as `BUILTIN_NOTE_TYPES` from the same module; use those constants instead of querying by name.

## Undo (POST /reviews/undo)

`POST /reviews/undo` atomically reverts the most recent grade for the authenticated user. The grade handler stores a `undo_snapshot` JSONB column (migration `0008_undo_snapshot.sql`) on the `reviews` row at write time, capturing the exact pre-grade card + profile fields. Undo reads the snapshot and restores those fields — including `updatedAt` — in a single transaction.

Error boundaries:
- Double-undo → 404 (snapshot cleared after first undo)
- NULL snapshot (legacy row) → 404
- Card mutated after the grade via `forget` or `setDue` → 409 (guards against restoring stale FSRS state)
- Suspended/leeched card — undo still works; leech state is part of the snapshot

Side-effect: restoring pre-grade `updatedAt` means the card won't appear at the top of `edited:` / `sort:updated` queries after an undo — this is intentional (it wasn't edited, it was reverted).

UI surface: `⌘Z` keyboard shortcut + explicit undo button inside the reviewer. Both dispatch the same store action.

## Forget / set-due (PATCH /cards/:id)

`PATCH /cards/:id` accepts two new optional fields alongside the existing patch surface:

- `forget: true` — resets the card to a fresh FSRS state via `fsrsResetColumns` (stability/difficulty/interval zeroed, state=New, lapses unchanged). Clears `suspended`.
- `setDue: <ISO string>` — overrides the `due` timestamp without touching any other FSRS field.
- Combining both in one request → 400.
- Invalid ISO date for `setDue` → 400.

The handler uses **explicit field mapping**, not a body spread, so unknown fields are silently ignored rather than applied to the row. Add new patchable fields to the explicit map if needed — never destructure the whole body into the update.

## Rich card content

Card fields are rendered as Markdown using `markdown-it` (`{ html: false, linkify: false }`) on the client, field-by-field inside `apps/web/src/components/render-card.tsx`. Syntax highlighting uses highlight.js in class-based mode (no inline styles); the theme lives in `apps/web/src/app/globals.css`. Mermaid diagrams are rendered asynchronously via the `RichCard` component (`apps/web/src/components/rich-card.tsx`) which uses a dedicated `MERMAID_DOMPURIFY_CONFIG` allow-list to permit SVG output.

**`RichCard` is the single card-render component** — used in the reviewer, card-form, note-type editor, and the cards browser. Never render raw field HTML outside of it.

Render pipeline order matters:
1. Math placeholders are substituted **before** markdown parsing (prevents `$…$` from being mangled by markdown-it).
2. Main DOMPurify sanitization runs on the markdown output.
3. Mermaid SVG islands are injected **after** main sanitization via a single `inject-node` pass (they cannot pass through the main sanitizer).

## Sanitizer (DOMPurify allow-list)

The main sanitizer allow-list was extended to cover rich Markdown output: `h1`–`h6`, `blockquote`, `pre`, `code`, `a`, `table`, `thead`, `tbody`, `tr`, `th`, `td`. The extension is symmetric — both the card-render edge and the note-type-editor preview edge use the same config, pinned by a bypass corpus in the test suite.

`<a href>` whitelisting is enforced via a `uponSanitizeAttribute` hook that permits only `https?:` and `mailto:` schemes. The previous `ALLOWED_URI_REGEXP` approach was removed in favour of the hook — do not re-introduce the regexp.

`style` attributes remain **blocked** in the main sanitizer. KaTeX and Mermaid output passes through isolated sink configs that allow `style` only for their respective output nodes (`KATEX_DOMPURIFY_CONFIG`, `MERMAID_DOMPURIFY_CONFIG`). `MEDIA_TOKEN_RE` is unchanged.

## Nested decks

`Deck.parentId?: string` gives Anki-style hierarchy. Tree helpers live in `apps/web/src/lib/decks.ts`: `buildDeckTree`, `flattenTree` (with `expanded: Set<string>`), `getDescendantIds`, `getDeckPath`, `aggregateCounts`, `canBeParentOf` (cycle guard), `deckPathLabel`. When rendering counts, **aggregate through the subtree** — a parent's "due" count includes descendants.

The `PATCH /decks/:id` handler also runs a cycle-guard on `parentId` changes. DB-level cascade takes care of deletes.

## Conventions & gotchas

- **Client-everywhere for screens:** every `(app)/*/page.tsx` is `'use client'`. Server Components that call `auth.api.getSession()` for SSR are OK outside the app group, but the app shell depends on client state.
- **`useSearchParams` + Suspense:** wrap consumers in `<Suspense fallback={null}>` (see `/editor` and `/auth/sign-in`).
- **Don't split screens into client+server halves** to save bundle size — dynamic topbar subtitles depend on the store.
- **Root `.env`:** the api loads it via `bun --env-file=../../.env` in its `dev` script; web via `apps/web/.env.local` which is a symlink to the root `.env`. Don't duplicate envs.
- **No default export from apps/api/src/index.ts** — Bun's auto-serve would collide with `app.listen()`.
- **Deck deletes cascade** at the DB level (FK `ON DELETE CASCADE`). The `decks.deleteConfirm` i18n message already spells out the subtree warning — keep it accurate if you change deletion semantics.
- **Remote cron trigger** (`trig_01VB9fKge65wEAm4VDzDDEEw`) runs hourly and opens PRs that fix non-working frontend UX (originally scoped to the Dexie era, still useful for mocked buttons / broken interactions). Small focused PRs from that agent are authoritative bug fixes — review and merge normally. If the PR references a feature that now depends on the backend, verify the API path actually exists before merging.
- **`AGENTS.md` is a verbatim mirror of this file** for Codex. After editing CLAUDE.md, `cp CLAUDE.md AGENTS.md` and restore the `# AGENTS.md` / "Codex" header at the top. Don't let them drift.
