import { NNOnboarding } from '@/components/screens/onboarding';

export default function Page() {
  return (
    <div style={{ minHeight: '100vh', background: 'var(--bg)' }}>
      <NNOnboarding />
    </div>
  );
}
