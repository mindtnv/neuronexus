# Второй проход: результаты реализации

Источник критериев: [аудит из 120 пунктов](spaced-repetition-audit-round-2.md). Реализация: `openspec/changes/complete-spaced-repetition-polish`. Незакрытые критерии остаются открытыми; этот отчёт не означает готовность всех 120 пунктов.

## R2-001 — закрыт

`sanitizeFieldValues` сохраняет Markdown как исходный текст, включая code/HTML-примеры, сравнения, entities и Unicode. Отдельная HTML-функция и её allowlist сохранены для явных HTML-вызовов. Client parser с `html:false` и обязательный DOMPurify sink защищают отображение; имена полей копируются без потери `__proto__`.

До исправления новые source tests воспроизводили потерю `<T>`, `<button>`, `<script>` и преобразование entities. После исправления POST → GET → PATCH → GET проверен через настоящий API/PostgreSQL; DOM-проверки сохранённого source не обнаруживают исполняемых script/svg/iframe/handlers или внешних img.

Проверка: Bun **1.3.14**, committed migration chain на изолированной `neuronexus_srs_test`; **173 passed, 0 failed**, 522 assertions в 6 файлах: notes, markdown-source API/web, sanitize API, sanitize-img API/web. Strict OpenSpec: 6/6. Это проверка данного среза, не финальный полный gate.

Уже потерянные прежней очисткой символы автоматически не восстанавливаются. Исправление производного поискового текста выполняется отдельно в R2-002.

## R2-013 — закрыт

Поля получают постоянные идентификаторы в существующем JSON; старые определения получают детерминированные legacy IDs при чтении. Переименование атомарно переносит ключи значений всех заметок и ссылки/условия шаблонов. Редактор сохраняет идентичность при перестановке и обновляет preview. Совпадение с уже сохранённым скрытым полем отклоняется до любых записей; ambiguous rename старого клиента без ID требует актуальное определение.

API-регрессия проверяет rename после grade, неизменные ID/due/reps/stability, отклонение повторяющихся имён и полную сохранность при коллизии. Четыре unit-проверки покрывают legacy IDs, секции, одновременную перестановку имён и коллизии.

## R2-015 — закрыт

Шаблоны имеют стабильные ID в JSON. Перестановка/переименование пересопоставляет ord по ID, сохраняя карточки и FSRS. Меняющиеся ord сначала освобождаются внутри транзакции, поэтому будущий unique constraint не помешает обмену позициями. Удаление используемого шаблона пока отклоняется с `template_removal_requires_preview`; полноценное подтверждаемое удаление и регенерация остаются открытым R2-014.

Красный тест показывал, как Forward получал вопрос Reverse с прежней историей. Зелёный проверяет обе стороны после одновременного rename и reorder. Обновления времени карточки монотонны. Новый schema column для JSON-идентификаторов не потребовался.

## R2-097 — закрыт

Миграция `0026_nebulous_havok.sql` делает исторический `reviews.deck_id` nullable с `ON DELETE SET NULL`. Владение review определяется пользователем и карточкой; удаление самой карточки по-прежнему удаляет её reviews. API/export допускают отсутствующую историческую колоду, клиентский Review и агрегирование обновлены.

Тест grade в A → move в B → delete A сначала терял review, после миграции сохраняет ID/rating/cardId с deckId=null и прежнее расписание карточки. Чужой пользователь не видит эту историю. После изменений: **37 API-тестов прошли**, 161 assertion (notes, note-types, review-safety); typecheck всех workspace прошёл. Миграция применена только к изолированной test-базе.

Откат R2-097: nullable FK можно оставить при откате совместимого приложения. Возвращать NOT NULL/CASCADE поверх появившихся null-строк нельзя без отдельного плана восстановления; не удалять исторические reviews ради отката. Ранее каскадно удалённые повторения не восстанавливаются этой миграцией.

## R2-002 — закрыт

Поисковые поля формируются из разобранного Markdown каждого поля, затем удаляется структура шаблона и однократно декодируется текст. Inline/fenced code сохраняет `<T>`, обратные слеши, pipes и буквальные entities. Изображение индексируется по alt, без storage URL; пустой alt не уничтожает действительный image-only вопрос. Обычный HTML в Markdown остаётся буквальным текстом, как и на экране.

Добавлен `apps/api/src/ai/rebuild-card-text.ts`: обязательная область userId, страницы по 200, условная запись по версии карточки, повторяемость. Опциональный `--reindex` дожидается существующего embedding worker batch, проверяет также неизменные строки при повторной попытке и использует штатную проверку capability/hash. API-тест проверяет чужую область, испорченный старый cache, повторный запуск, неизменные source/due/reps/updatedAt. Без флага платных запросов нет.

## R2-003 — закрыт

Общий `cardMathPlugin` разбирает математическую разметку внутри Markdown grammar. Inline, fenced и indented code остаются буквальными; literal HTML attributes и URL не становятся формулами. Обычные формулы, в том числе display math с пустыми строками, проходят тот же безопасный KaTeX sink.

Новые красные проверки воспроизводили `.katex` внутри code и потерю delimiters. После исправления пройдены функциональные и security corpora: 190 тестов shared/render/sanitizer, затем дополнительные display-block проверки. Typecheck всех workspace прошёл.

## Контрольная точка после шести пунктов

Закрыты R2-001, R2-002, R2-003, R2-013, R2-015, R2-097. На закреплённом Bun 1.3.14 полный migration-faithful `test:ci` после изменений: **2221 passed, 0 failed**, 6548 assertions, 142 файла, 67.95 с. Полные финальные ворота change остаются открытыми до реализации остальных пунктов. Осталось 114 критериев; закрытие шести не означает готовность всего второго прохода.

Production build API/web выполнен успешно (`NODE_ENV=production bun run build`); strict OpenSpec 6/6, typecheck всех workspace, `git diff --check` и синхронность чекбоксов/инструкций проверены. Миграционные SQL/metadata находятся в рабочем дереве; коммит/публикация/production deploy не выполнялись. Следующий срез — безопасная регенерация с impact-preview (R2-014/028), необходимая для полноценной нумерованной cloze-модели R2-004.

## R2-014 — безопасная регенерация типа

Один общий planner сопоставляет существующие карточки с будущими по стабильной идентичности. Preview показывает create/keep/delete, число удаляемых reviews и до 50 конкретных удаляемых вопросов. Применение создаёт недостающие карточки, сохраняет ID/FSRS оставшихся и удаляет только подтверждённые. Удаление или превращение вопроса в пустой больше не оставляет карточку без действующего шаблона. Заметке без карточек для выбора колоды требуется отдельное восстановление (R2-080); операция не пропускает её молча.

`POST /note-types/:id/preview` не записывает данные. PATCH проверяет hash текущего плана и его исходных строк; grade между preview/apply делает прежнее согласие недействительным. React-тест проверяет конкретный вопрос/число повторений, защиту от двойного клика и передачу того же token. Добавление/удаление шаблона проверены через реальный API/БД с сохранением расписания оставшейся карточки.

## R2-020 — согласованность открытых карточек

После изменения типа одним обновлением store публикуются новое определение и свежие карточки: первая страница данного типа плюс bounded lookup уже загруженных глубоких карточек. Удалённые карточки исключаются. Последующая операция над типом и reset аккаунта отменяют право старого ответа обновить зеркало. Read generation не позволяет старому search/refetch воскресить удалённые строки. Browser повторяет авторитетный поиск после смены каталога типов.

Раскрытый reviewer сохраняет показанный вопрос/ответ, блокирует оценку и предлагает обновить очередь при изменении его типа. Проверены deep-card refresh, новые/удалённые карточки, delete во время старого refresh, ошибка фонового чтения и поздний search. API lookup принимает максимум 200 UUID, дедуплицирует их и не возвращает чужие карточки; фильтр noteTypeId также user-scoped.

## R2-021 — обновление индекса после изменения типа

После успешного commit enqueued только новые карточки и реально изменившийся renderText. No-op/style-only изменения не запускают enqueue; удаление карточки каскадно удаляет kb_chunk. Интеграционная проверка со spy на существующей очереди и настоящим kb_chunk подтверждает отсутствие enqueue у preview/style-only, точные изменившиеся ID и удаление старого индексного ряда.

## R2-024 и R2-026 — конфликты редакторов

Типы и заметки имеют передаваемую версию updatedAt. Старый редактор получает 409 вместо перезаписи новых данных. Его draft остаётся на экране; отдельно показаны актуальные поля/шаблоны для сравнения и копирования. Замена draft либо осознанное принятие новой основы требует явного действия. Следующее сохранение снова проходит preview. Собственный успешный save обновляет базовую версию формы.

API-тесты проверяют two-editor overwrite и неизменность текущих данных; React-тесты — сохранение локального текста, показ другой версии и отправку новой expectedUpdatedAt только после решения пользователя.

## R2-028 — удаление дочерних карточек при правке заметки

`POST /notes/:id/preview` считает реальную историю, в том числе сохранённые reviews после ручного Forget. Изменение полей, убирающее вопрос, требует того же consistency token в PATCH. Простой текстовый edit без удаления не вызывает диалог. Отмена preview оставляет draft и не отправляет write. Проверены read-only preview, отказ без token, stale token после grade и успешное подтверждённое удаление.

AI-путь также сохраняет server-generated token в pending tool-call JSON, не принимает его из model args и сверяет при resume. Изменение заметки после предложения приводит к отказу без мутации. Поля и перенос карточки применяются в общей транзакции; устранено ожидание собственного lock при обращении к глобальному db вместо tx. Пройдены 11 agent-confirm тестов, включая stale confirmation и совместное edit+move.

## R2-100 — согласование типа и записи заметки

Внутренние create/update проверяют версию определения после получения lock; порядок — type → note → cards. Все вызовы, включая AI и reading/harvest, передают версию, по которой генерировали карточки. Устаревшее разрешение create/update отклоняется до записей. При удалении типа последующая запись не возвращает исчезнувшую заметку. Контролируемые interleavings тестируют resolve → type change/reorder/delete → commit; результатом является согласованное состояние либо conflict, без частичной записи.

Дополнительно уточнён R2-002: PostgreSQL хранит микросекунды, JS Date — миллисекунды. Условный backfill теперь сравнивает поддерживаемую миллисекундную версию и чинит также never-reviewed строки; специальный тест использует timestamp `.123456` и проверяет сохранение reps=0.

### Проверки второго среза

Полный migration-faithful `test:ci`: **2242 passed, 0 failed**, 6644 assertions, 146 файлов, 70.62 с. Typecheck всех workspace, strict OpenSpec 6/6 и diff whitespace прошли. После этого добавлена отдельная защита от late search после удаления типа; её результат и финальная сборка фиксируются ниже. Итоговые ворота всех 120 пунктов остаются открытыми.

### Контрольная точка: 13 из 120

Закрыты ещё R2-014, R2-020, R2-021, R2-024, R2-026, R2-028 и R2-100; чекбоксы аудита и OpenSpec синхронизированы. Приёмка после доработок: **2244 passed, 0 failed**, 6649 assertions, 146 файлов, 69.18 с. Исправлен найденный полным прогоном конфликт с прежним поиском: строки сохраняются и обновляются из свежего зеркала, удаляются только исчезнувшие карточки. После типовой правки mock fetch typecheck всех workspace и production build API/web прошли.

Форма типа блокирует редактирование во время preview/apply; уход со страницы не открывает запоздавший диалог и не отправляет ещё не подтверждённую запись. Это покрыто новым React-тестом. Полноценные восстанавливаемые drafts остаются отдельным открытым R2-025.

Осталось **107** пунктов. Следующий срез — нумерованные cloze, их грамматика и безопасный переход существующих карточек; все общие финальные ворота change ещё открыты. Production не изменялся.

## R2-004 — независимые cloze-вопросы

Новая заметка создаёт отдельную карточку для каждого номера на лицевой стороне шаблона. Повторяющийся номер объединяет пропуски; у каждого вопроса собственные ID и FSRS. Добавление номера не пересоздаёт прежние карточки, удаление номера проходит обычный preview с подсчётом затронутой истории.

Миграция `0027_dazzling_madame_masque.sql` маркирует прежние общие вопросы числом 0 без изменения остальных полей. Они остаются общими до явного разделения. В preview пользователь выбирает наследника истории отдельно для каждого старого шаблона (`clozeRetainHistoryFor`); выбранный вопрос сохраняет ID/историю/расписание, остальные начинают с нуля. Неполное отображение или смена решения без нового token отклоняется. Старые записи без номера от прежнего API также распознаются как общие. Старое c0 остаётся читаемым, хотя новые независимые вопросы требуют положительных номеров.

API-регрессии проверяют независимую оценку c1/c2, сохранение ID при добавлении c3, удаление ровно нужной истории, разделение 0/null legacy, разные номера в нескольких шаблонах и stale type version. В UI выбор истории явно подтверждается кнопкой разделения, а не ложной кнопкой удаления.

## R2-005 и R2-006 — грамматика и буквальные примеры

Вместо плоской регулярки работает ограниченный сбалансированный разбор: подсказка отделена от ответа, фигурные скобки и вложенные пропуски не обрываются. Допускается до 8 уровней и 128 сгенерированных вопросов на заметку; превышение отклоняется явно. Формулы с cloze преобразуются до KaTeX и не раскрывают ответ на вопросной стороне. Общая глубина учитывается и на границе Markdown/math.

Inline/fenced/indented code остаётся буквальным. Номера поступают из разобранных полей, реально используемых лицевой стороной, а не из HTML-атрибутов шаблона: добавленный регрессионный пример с `data-nn-cloze="99"` больше не создаёт ложный вопрос. Некорректный новый текст не записывается; некорректный старый показ выдаёт понятную ошибку без падения и частичного раскрытия ответа. Скрытые и неиспользуемые Mermaid-блоки не рендерятся заранее.

## R2-029 — полный предпросмотр

Редактор показывает число фактически создаваемых вопросов и позволяет выбирать любую пару шаблон/номер, включая обратную карточку. При переключении ответ снова скрыт. Пропущенные необязательные шаблоны перечислены с объяснением. React-тесты проверяют c1 → раскрытие → c2, обычный reverse и выбранное направление переноса legacy-истории.

## R2-099 — уникальность вопроса

Введён versioned `UNIQUE NULLS NOT DISTINCT (note_id, template_ord, cloze_number)`: в текущей версии типа каждой позиции соответствует один стабильный ID шаблона; смена позиций выполняется атомарным сопоставлением этих ID. Один вопрос не может появиться дважды, обычное null-значение также защищено. Перестановки шаблонов и разделение cloze проходят под этим ограничением.

Проверена полная миграция на отдельно созданной временной БД: цепочка до 0025 → старый aggregate с расписанием и review → намеренный дубль → отказ 23505 с сохранением обеих строк и истории → удаление только синтетического дубля → успешная и повторная миграция. JSON-снимки всех старых полей карточки и истории полностью совпали по значениям. Временная БД удалена самим проверочным сценарием. SQL-регрессии в `cloze-migration.test.ts` повторяют сохранность/отказ на изолированных transaction-local таблицах; API-тест проверяет сам constraint.

## R2-008 — честное оформление

Убрана возможность редактировать CSS, который всё равно не применялся. Интерфейс объясняет, что карточки используют тему приложения. Ранее сохранённый CSS доступен только для чтения, сохраняется при правках типа и остаётся в экспорте. Проверка подтверждает отсутствие редактируемого CSS-контрола и сохранение исходного значения в PATCH; произвольный CSS в рендер не добавлялся.

## R2-012 — темы диаграмм

Mermaid использует default/light либо dark по фактическому `data-theme-mode` и наблюдает смену семейства. Старый асинхронный результат не может заменить новую диаграмму. Пока идёт построение, показывается локализованная загрузка, а не внутренний placeholder ID. SVG проходит прежний sanitizer.

Проверены live theme switch и delayed-result race в React, плюс 99 renderer/security тестов. Реальная конфигурация установленного Mermaid даёт контраст базового текста/узла **16.07:1** для default и **12.28:1** для dark. Реальный тёмный SVG «Повтор → Память» проверен визуально в браузере. Это не заменяет отдельные будущие проверки VoiceOver/физического iOS.

### Контрольная точка: 20 из 120

Добавлены R2-004, R2-005, R2-006, R2-008, R2-012, R2-029 и R2-099. Полный migration-faithful прогон: **2280 passed, 0 failed**, 6765 assertions, 150 файлов, 72.21 с. Обязательные реальные S3 round trips: **16/16**. После уточнения типа Mermaid mock прошли typecheck всех workspace, production build API/web, strict OpenSpec 6/6 и whitespace check. Исправлена независимость DOM-тестов от порядка запуска: harness восстанавливается перед каждым renderer suite.

Локальная dev-база `neuronexus_srs_dev` проверена по адресу/имени перед применением миграций. Подняты API :3000 и production standalone web на loopback :3001, Node 26.8.2. В изолированном аккаунте Study QA через настоящий браузер создана заметка с тегом `qa-round2`: два вопроса, разные скрытые пропуски, диаграмма только после раскрытия. После одной оценки первый вопрос получил расписание, второй остался новым; итог — **1 ответ / 1 карточка / +10 XP**. В browser console нет errors/warnings. Синтетическая заметка оставлена для дальнейшей проверки; production не изменялся.

Осталось **100** пунктов. Общие финальные ворота change не закрыты: этот результат относится только к перечисленным срезам.


## R2-007 — пригодность медиа-вопроса

Генератор и браузер используют одинаковый Markdown image hook: поддерживаются `/m/<uuid>`, внешний URL не превращает alt в невидимый вопрос. Markdown-вопрос из одной валидной картинки без alt сохраняется; один raw HTML `<img>` даёт понятную ошибку формата без потери исходника. Пустые дополнительные reverse-шаблоны по-прежнему пропускаются. Проверено shared-регрессиями и полным набором render/sanitize/media тестов.

## R2-009 и R2-010 — явный ответ Type-in

`typeinAnswer` закрепляет роль за существующим стабильным ID поля. Добавление, перестановка и переименование полей не меняют ответ. В редакторе типа есть выбор поля; legacy JSON миграция 0027 закрепляет прежнее последнее поле по ord, не переставляя поля и не меняя содержимое заметок. SQL-проверка выполняет фактическую миграцию на временных таблицах, проверяет сохранность данных, уже выбранной роли и повторное выполнение backfill.

Ожидаемый ответ извлекается общим `fieldPlainText`: Markdown-разметка и URL ссылки не входят в ответ. Заметка хранит `accepted_answers`, редактор показывает канонический текст и принимает до 20 явных вариантов по 256 символов. Варианты сохраняются после перезагрузки, обычных API/AI-правок и входят в токен preview. Сравнение нормализует только NFC, регистр и пробелы по краям: близкое написание не принимается автоматически, оценку по-прежнему выбирает человек. Назначение поля встроено в DTO карточки с версией типа; вспомогательный Extra не влияет на сравнение.

## R2-016 — однозначные имена полей

Общая UI/API-проверка нормализует trim/NFC, отвергает пустые/повторяющиеся без учёта регистра имена, фигурные скобки, управляющие символы и конфликтующие префиксы синтаксиса. API сохраняет нормализованные ссылки. AI-сопоставление ключей следует той же нормализации и отвергает неоднозначные дубли вместо перезаписи. `Object.fromEntries` сохраняет значения специальных имён; отсутствующее prototype-named поле ответа не превращается в унаследованную функцию или 500.

## R2-017 — проверка шаблонов на существующих заметках

UI/API находят неизвестное поле, непарные/повреждённые условия, чрезмерную вложенность и пустую статическую лицевую сторону. Ошибка UI называет шаблон, сторону, позицию и поле. Preview проверяет все принадлежащие владельцу заметки, возвращает максимум 5 коротких примеров и список пропущенных optional-шаблонов. Непригодная заметка блокирует apply, даже с правильным токеном подтверждения. Изменение на существующих заметках показывает примеры перед применением; пустой reverse разрешён, если остаётся пригодный вопрос. API- и React-проверки доказывают блокировку без удаления и сохранность черновика.

## R2-030 — пригодность по фактическому вопросу

Удалено требование заполнить ord 0. UI использует тот же `generateCards`, что API, AI и чтение источников; custom-вопрос из более позднего поля сохраняется при пустом первом служебном поле. Некорректный cloze, пустой Type-in ответ и заметка без генерируемого вопроса отклоняются. PATCH не может оставить заметку без карточек. Добавлена React-регрессия настоящего сохранения такого custom-вопроса, API-проверки и общие тесты генерации.

## Проверка среза — 26/120

- Bun **1.3.14**: migration-faithful `test:ci` — **2296 passed, 0 failed**, 6827 assertions, 153 файла. После дополнительного условия на пустую секцию шаблона — повторная целевая проверка **11/11**, 49 assertions.
- Mandatory real-S3 round trips — **16/16**. Typecheck всех workspaces, production API/web build, strict OpenSpec **6/6**, `git diff --check` пройдены.
- Миграция 0027 применена к изолированным `neuronexus_srs_test` и `neuronexus_srs_dev`; production не изменялся.
- Реальный браузер, локальная standalone-сборка: создана карточка «Столица Франции?» / `**Paris**` с вариантами «Париж» и `Lutetia`; после reload варианты сохранены. Ввод `париж` отображается как точное совпадение без зачёркиваний, канонический ответ — `Paris` без Markdown. Выбрано «Хорошо», завершено занятие: **1 ответ, +10 XP**. Console errors/warnings: **0**. Синтетическая карточка `01a0b981-6cd9-7439-a5ed-74a3a2357513` оставлена для дальнейших проверок.

Закрыты 26 пунктов; **94 остаются открытыми**. Общие финальные ворота change не закрыты до завершения остальных критериев.


## R2-011 — ошибки rich content с контекстом

Рендерер сохраняет поле и номер блока для Mermaid и формул. Неверная формула больше не исчезает; ошибочный блок показывает локализованную подсказку и экранированный исходник внутри существующего SafeHtml sink. Ошибка одного блока не скрывает соседний текст/исправную диаграмму. Контекст и исходник скрытой стороны не раскрываются. Mermaid не добавляет собственный error SVG в документ (`suppressErrorRendering:true`); исходники и parser exceptions не пишутся в console. Исправление source восстанавливает обычное отображение. Клик по блоку ошибки не переворачивает вопрос.

Проверки: **99/99** renderer/KaTeX/sanitizer/React tests, **42/42** reviewer/editor lifecycle tests. Новые тесты покрывают два блока одного поля, скрытый ответ, небезопасные строки в исходнике, сочетание плохой и хорошей диаграммы, восстановление формулы после редактирования и сохранность reveal/grade/edit.

В реальном браузере создана заметка `qa-rich-diagnostics`: ошибочные `\notARealCommand{x}` и Mermaid с незакрытой подписью, обычный текст до/после. Предпросмотр и занятие показывают «Поле Front · формула 1 / диаграмма 1», исходники и подсказки; текст виден, ответ раскрывается, четыре оценки доступны. Из занятия выполнен переход «править»: тот же ID и исходник остались в редакторе. Console errors/warnings: **0**.

## R2-018 — отдельный переход режима ответа

Обычный PATCH больше не меняет kind. Добавлены owner-only `/note-types/:id/kind/preview` и `/kind`: обязательны версия типа, выбранное поле для Type-in и точный токен текущего воздействия. В UI отдельный экран через «Изменить режим» объясняет область и показывает реальные примеры/счётчики до подтверждения.

Basic ↔ custom сохраняет способ ответа, ID карточек, FSRS и историю. Переходы через cloze/typein создают свежие вопросы: удаление прежних карточек и повторений **явно раскрывается до применения**, source заметок остаётся прежним; история не переносится к другому способу ответа. Весь переход атомарен. Cloze без пропусков и Type-in с полем ответа на вопросе либо без него на обороте блокируются. Новая оценка после preview, конфликт версии и чужой тип отвергаются без изменения данных. Для глобального типа сначала нужна собственная копия.

API-тесты проверяют оба класса переходов, сохранность расписания/истории обычного вопроса, точные destructive counts, свежее расписание нового режима, непригодные данные, owner isolation и устаревшее подтверждение. UI-тест доказывает отдельный preview → confirm → apply с тем же токеном и выбранным полем.

Нативная проверка: встроенный Basic скопирован в «Проверка формата»; в нём создана заметка с rich-content диагностикой, затем выполнен Basic → custom. Preview: **создать 0, сохранить 1, удалить 0, удалить повторений 0**. Карточка `01a0b998-4412-7088-9714-ae753c7ffd69` сохранила ID и текст, открылась в занятии и редакторе. При браузерной проверке исправлена устаревшая подсказка, ошибочно называвшая первое поле обязательным (дополнение к R2-030).

## Проверка среза — 28/120

Bun **1.3.14**, migration-faithful full suite: **2307 passed, 0 failed**, 6896 assertions, 154 файла. Mandatory real-S3: **16/16**. Typecheck всех workspaces, production API/web build, strict OpenSpec **6/6**, diff check — пройдены. Новых миграций в этом срезе нет. Production не изменялся; локальная standalone-сборка остаётся доступной на `127.0.0.1:3001`.

**92 пункта остаются открытыми.** Перенос выбранных заметок на копию типа (R2-019/R2-031) ещё не реализован; его нельзя считать закрытым только по clone-on-edit или смене kind.


## R2-019 — применение копии встроенного типа

После clone-on-edit показано, что прежние заметки пока остаются на исходном типе, и предложен переход к выбору этих заметок. Список фильтруется по точному `noteTypeId`, а не по потенциально неоднозначному имени. Целевой тип уже выбран в диалоге преобразования. У своих типов есть постоянное действие «Применить к заметкам», доступное и после перезагрузки. Глобальное определение и невыбранные заметки не изменяются.

Реальный браузер: Basic скопирован в «Копия для переноса» (`01a0b9bd-5d8c-7793-8b9c-db37837e244d`), поле Front переименовано в Question. Через предложенный переход выбрана существующая заметка «Сколько будет 2 + 2? Проверка возврата.»; явно сопоставлены Front → Question и Card 1 → Card 1. Preview: **сохранить 1, создать/удалить 0, удалить повторений 0, потерянных значений 0**. После применения карточка видна в общей коллекции с новым типом и полем Question; полный Markdown-ответ, состояние «Повтор» и дата 20 сентября сохранены. Общее число карточек остаётся 7.

## R2-031 — преобразование заметок с отображением полей и истории

Новый owner-scoped preview/apply путь ограничен 200 заметками и 2000 существующими/результирующими карточками. Версии обоих типов обязательны; токен связывает определения, заметки, полевые/шаблонные соответствия и текущее состояние карточек/повторений. Внутри одной транзакции меняются тип заметки и производные карточки; source/target types блокируются в одном порядке, затем заметки и карточки. Изменённый поисковый текст переиндексируется после commit с request correlation.

Поля сопоставляются явно. Несопоставленные значения сохраняются по умолчанию, включая дополнительные ключи; удаление требует явного выбора и показывается с количеством/примерами. Коллизия не перезаписывает прежнее значение. Сопоставление шаблонов использует стабильные IDs, а не их позицию; совместимые направления, включая независимые номера cloze, сохраняют свои карточки, FSRS и отзывы. Новые/несовместимые вопросы и устаревшие альтернативы Type-in раскрываются до подтверждения. Preview показывает соответствия карточек, поля, удаляемые вопросы/повторения и сокращённые примеры обеих сторон. Одна непригодная заметка блокирует весь пакет.

Диалог доступен в отдельном редакторе и в массовом выборе браузера. Он объясняет, что операция включает все направления заметки. Несохранённый черновик защищён; для массового действия сначала требуется закрыть редактор. После успешного ответа локальные строки сразу согласуются с результатом, даже если повторная загрузка списка не удалась. После неопределённой ошибки apply предлагается обновить фактическое состояние, а не повторять действие по старому выбору. Поздние ответы после выхода/смены аккаунта не восстанавливают старый интерфейс.

Дополнительный браузерный путь: карточка `01a0b998-4412-7088-9714-ae753c7ffd69` преобразована из «Проверка формата» в ту же копию через отдельный редактор. **URL и ID сохранены**, тип и поля обновились сразу, без reload, исходник ошибочной формулы/диаграммы остался в Question. Console errors/warnings: **0**. Синтетические данные оставлены для дальнейшего QA; production не изменялся.

## Дополнение к R2-017/R2-030 — невидимые HTML-контейнеры

Воспроизведён реальный расходящийся результат: для `<script>hidden</script>` генератор возвращал одну карточку, а display renderer — пустую строку. Общая валидация теперь отвергает контейнеры, чьё содержимое удаляет display sanitizer, и незавершённые теги. UI указывает шаблон, позицию и тег. Обычные wrappers и экранированные HTML-примеры в Markdown-полях остаются рабочими; комментарии не попадают в поисковый текст и не считаются медиа-вопросом.

Целевой набор (API conversion/definition, shared validation, Markdown, sanitizer, math, conversion UI): **134/134**, 568 assertions. Этот запуск также воспроизвёл зависимость DOM-тестов от порядка загрузки: DOMPurify мог импортироваться после teardown предыдущего набора и до нового DOM. Тесты теперь регистрируют среду перед динамическим импортом DOM-зависимых модулей; production-код для этого не менялся.

## Синхронизация с main — 2026-09-19

Работа сохранена в checkpoint `820510e` и объединена с `origin/main` (`59c1fe8`): MCP, новый layout, Bun 1.4.2 и совместимые обновления зависимостей. Новый sidebar сохраняет серверные SRS-счётчики. Миграция MCP `0025_overconfident_ghost_rider` остаётся без изменений; наши миграции перенумерованы в `0026_nebulous_havok`, `0027_dazzling_madame_masque`, `0028_broad_blizzard`, SQL и временные метки сохранены. Все 29 snapshots связаны, `db:generate` не находит расхождений.

При совмещении исправлена интеграция MCP `edit_card`: preview использует транзакцию вызывающего кода, блокировки идут type → note → cards, выполнение получает токен только из сохранённого и повторно проверенного preview. Добавлена проверка отказа при изменившейся истории повторения и успешного применения нового preview с сохранением карточки/FSRS.

Проверки объединённой версии: миграции применены с нуля к отдельной тестовой базе; `test:ci` — 2345 passed, 0 failed (157 файлов); обязательный реальный S3 — 16/16; typecheck, production build и strict OpenSpec — успешно. В браузере проверены редактор с перенесённой заметкой, новый layout, 7 сохранённых QA-карточек и очередь из 4 доступных карточек. Локальная dev-база предварительно сохранена через pg_dump; недостающая MCP-миграция добавлена атомарно после сверки хешей уже применённых SRS-миграций. Production не изменялся.

После сверки реализации и объединённого полного прогона R2-019/R2-031 отмечены выполненными: **30/120**, ещё 90 открыты. Срез 28/120 выше описывает состояние до реализации преобразования.

## R2-022 — стоимость изменения популярного типа

Синтетический, изолированный от dev/production сценарий PostgreSQL 18: **10 000 заметок / 10 000 карточек** одного типа. До изменения preview занимал **200,9 мс**, применение — **3138,3 мс** в одной неограниченной транзакции. После добавления admission-проверки запрос отклоняется **до загрузки/блокирования всех заметок**: **23,1 мс**, HTTP 413. Определение, все карточки и заметки остаются прежними. Допущенные **1000 заметок / 1000 карточек** прошли preview + apply за **425,4 мс** с сохранением всех ID. Это локальные замеры, не обещание production latency.

Пределы синхронного изменения: 1000 заметок, 2000 существующих/результирующих карточек, 2 MiB исходного, развёрнутого по ссылкам полей или итогового поискового текста. Повторные вставки большого поля в шаблон проверяются до построения огромной строки. Внутри транзакции: ожидание блокировки 750 мс, SQL statement timeout 2 с, monotonic deadline работы 5 с с проверками между генерацией вопросов и записями. Уже выполняющийся SQL-запрос может использовать оставшуюся часть своего 2-секундного таймаута; проверка перед commit всё равно откатывает просроченную операцию. Это ограничение одной атомарной операции, без worker и без частично применённых определений.

Тесты отдельно проверяют число заметок, число существующих/новых вопросов, объём исходника, повторное разворачивание большого поля, конфликт блокировки и принудительный SQL timeout **после записи нового определения**. Последний подтверждает полный rollback определения и карточек; ID для переиндексации очищаются. Неизменённые поля/шаблоны не запускают перерасчёт, поэтому переименование большого типа остаётся доступным.

Редактор оставляет исходный draft, различает превышение размера и временно занятую коллекцию. «Сохранить как копию» доступно для собственного типа и ведёт к выбору заметок через существующий preview преобразования. В браузере собственный тип «Копия для переноса» сохранён как «Копия для пакетного изменения» (`01a0ba32-4421-71d9-9186-52efa0811980`). Показано, что прежние заметки пока не изменены; переход открыл исходные 2 карточки с заранее выбранной целью. Общее число QA-карточек осталось 7, фактическое преобразование повторно не запускалось.

Проверка среза **31/120**: итоговый `test:ci` — **2354 passed, 0 failed**, 7341 assertions, 158 файлов; обязательный реальный S3 — **16/16**; typecheck всех workspaces, production build API/web и strict OpenSpec **7/7** — успешно. Новых миграций нет; тесты используют отдельную базу, ранее созданную полной цепочкой миграций, без schema push. **89 пунктов остаются открытыми.**

## R2-023 — полные последствия удаления типа и сохранение материала

Добавлен отдельный `GET /note-types/:id/delete-preview` с `Cache-Control: no-store`. Read-only repeatable-read snapshot считает **все** принадлежащие пользователю заметки, карточки и повторения, включая заметки без карточек. Клиентский mirror и его предел 500 не участвуют. SQL возвращает только агрегаты и хеши состояния; исходники и неограниченный массив ID не передаются в приложение ради подсчёта. Токен SHA-256 связывает операцию, владельца, определение, состав/содержимое заметок и карточек, историю повторений. Замена заметки при прежнем общем количестве тоже делает подтверждение устаревшим.

`DELETE /note-types/:id` теперь требует `{ confirmationToken }`, в том числе для неиспользуемого типа. В одной транзакции блокируются существующий профиль → тип → заметки → карточки → повторения, проверяется актуальный снимок и выполняется каскад. Новая оценка, изменение исходника или преобразование заметки после preview дают 409 без удаления. Глобальные и чужие типы — 404. Те же ограничения ожидания/SQL/работы, что у редактирования, обеспечивают полный rollback при таймауте. Тест намеренно задерживает AFTER DELETE trigger карточки и подтверждает сохранность типа, заметок и карточек после отмены каскада.

Диалог получает серверные числа до включения действия удаления. Предлагает преобразовать заметки через список с точным `noteTypeId` или скачать существующий JSON-экспорт аккаунта; заметки без карточек явно отнесены к экспорту. Ошибка preview блокирует удаление. Устаревшее подтверждение сбрасывается и требует отдельного обновления последствий и нового решения. При потере ответа предлагается обновить фактическое состояние; старый token не переиспользуется. Поздний ответ после закрытия не возвращает диалог. Экспорт не запускает DELETE; перенос использует уже проверенный field/card mapping.

API-проверки: 701 заметка, 700 карточек, 700 повторений и чужая коллекция; read-only preview; отказ без токена; чужие/глобальные/несуществующие типы; новая оценка и смена исходника; одинаковые counts при новом составе; преобразование одной заметки перед удалением прежнего типа с сохранением ID/FSRS/истории; rollback каскада. В тесте большого scope вложенный подзапрос истории воспроизводимо достигал 2-секундного statement timeout; заменён прямыми FK JOIN без повышения таймаута. Сценарий с fixture/preview/удалением прошёл за 441 мс.

UI-проверки: double click, ошибка загрузки, обновление stale preview, потерянный ответ, экспорт JSON с persisted orphan note, поздний ответ и реальные числа EN/RU при пустом локальном mirror. В браузере для «Копия для переноса» показаны **2 заметки / 2 карточки / 3 повторения**. Кнопка сохранения открыла ровно исходный тип, выбор карточки — диалог с выбором нового типа. Исправлена подсказка source-only scope, ранее показывавшая пустое имя цели. Деструктивная кнопка в браузере не нажималась; QA-коллекция сохранила 7 карточек.

Проверка среза **32/120**: `test:ci` — **2368 passed, 0 failed**, 7426 assertions, 160 файлов; обязательный S3 **16/16**; typecheck и production API/web build — успешно; strict OpenSpec **7/7**. Новых миграций нет. API следует выкатить до web: старый клиент безопасно получает отказ без preview token, новый клиент не может удалить через старый API без отдельного preview endpoint. Улучшения формата/медиа/восстановления экспорта остаются отдельными открытыми пунктами. **88 пунктов ещё открыты.**

## R2-025 — восстановление незавершённых черновиков

Редакторы заметок и типов сохраняют локальные записи v1: владелец, note/type ID (отдельный `new` для создания), revision UUIDv7, время и полный исходник с исходной версией. Поля Markdown, теги, выбранный тип/колода, альтернативы Type-in и выбор наследника legacy-cloze не обрезаются. Для заметки сохраняется определение типа, по которому автор редактировал текст; его обновление после reload не подменяет исходную версию. Для типа сохраняются поля, шаблоны, styling и kind. Это локальный recovery, не офлайн-запись на сервер.

Пределы: 256 KiB сериализованного UTF-8 на запись, 10 записей / 1 MiB на аккаунт. Прежняя работа не вытесняется при переполнении. Autosave через 300 мс дополняется flush на pagehide, beforeunload и unmount. Ошибка storage/лимита видна; неподтверждённый текст остаётся в форме, может быть скачан прямо из памяти, а недоступное сохранение защищает выход native beforeunload. Управление заполненным хранилищем можно открыть в новой вкладке, не покидая текущий несохранённый текст.

После загрузки предлагаются «Восстановить» / «Отбросить»; до выбора нельзя записать поверх найденного черновика. Восстановление не запускает API-запись. Старые source/type versions участвуют в обычном конфликтном workflow; новые определения не скрывают сохранённые дополнительные поля. Исходник этих полей остаётся видимым, его можно перенести в нужное поле или явно убрать. Для существующей заметки восстановление неизменённого выбора колоды использует текущую колоду открытой карточки, вместо неявного переноса её направлений назад.

App navigation, ссылки, выход из аккаунта, prev/next, закрытие dock и переход к похожей карточке проверяют незавершённые изменения: сохранить на сервере / оставить локально / отбросить / остаться. Неуспешная запись удерживает редактор. Native history/reload сохраняет текст через lifecycle flush; исходная карточка также доступна через ссылку в списке черновиков. Bootstrap завершается до появления редактируемой формы; смена владельца/карточки переоткрывает её состояние. Поздний подтверждённый save очищает только исходный совпадающий revision, не новый черновик и не другой аккаунт. Отдельно закрыта утечка late `addNoteType` в зеркало следующего аккаунта (часть R2-101; сам большой пункт остаётся открытым).

`/editor?drafts=1` показывает только записи текущего владельца, включая текст исчезнувших заметок/типов. Доступны просмотр исходников, JSON-скачивание и явное удаление конкретного draft с проверкой revision. Повреждённые/чужие envelopes не раскрывают содержимое. Удаление оригинала не отнимает доступ к локальному тексту; при этом восстановление серверной истории не обещается. В карточках и списке типов есть прямой переход к черновикам.

Тесты покрывают lifecycle-flush/reload, создание отдельно от редактирования, точный source и версии, смену аккаунта, поздний save, другой card direction той же заметки, failure→success cleanup, отмену/keep/discard/save при уходе, возврат к исходному тексту, отказ хранилища, stale revision, квоты без удаления прежних записей, type-draft, orphan-source download и локализованные состояния. Используется реальный React + storage API тестовой DOM-среды; API-тайпчекинг и регрессии SRS сохранены.


Native QA: в редакторе и панели карточки проверены keep → повторное открытие → restore → reload → подтверждённый save → очистка draft. Восстановленный исходник совпал полностью; временные QA-изменения возвращены к исходному тексту. Отдельная регрессия сохраняет восстановленный текст, даже если он совпал с устаревшим cached baseline перед обновлением карточки. R2-025 закрыт: **33/120**, остаётся **87**.

## Подготовка релиза с Reomi

Совмещено с `origin/main` `f3147c1` (Reomi): сохранены новая дизайн-система, шрифты, темы, таблица с выбором колонок, боковая панель и inspector повторения. В них встроены SRS-счётчики с сервера, scope/undo/error состояния, numbered-cloze, Type-in, безопасные преобразования и локальные drafts. Chat/MCP сохраняет оба серверных механизма подтверждения: impact snapshot и regeneration token. Миграции main не изменены; добавляются только 0026–0028. Deploy ждёт завершения и здоровья API до старта web.
