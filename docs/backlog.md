# Product backlog

## P4 — someday / lowest priority

- [ ] **Run code blocks and inspect their output.** Add an optional Run action to supported fenced code blocks in cards, with a separate result/error area. This is deliberately far-backlog work: no implementation or scheduling commitment. Before planning it, choose supported languages and an isolated execution environment with strict runtime/memory limits, cancellation and no implicit access to user data or network. Code must run only on an explicit user action; rendering a card must never execute it. Requested 2026-09-19.
