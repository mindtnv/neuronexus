# Generated by @serwist/next on every build (ralplan Phase B — inert SW).
# These are build artifacts, not source; gitignored so builds don't dirty the
# tree or create false positives against the sanitizer `git diff --exit-code`.
public/sw*
public/swe-worker*
