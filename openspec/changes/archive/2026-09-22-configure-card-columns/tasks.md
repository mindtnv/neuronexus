# Tasks

## 1. Column preferences
- [x] 1.1 Add failing tests for defaults, required Question, unknown/corrupt preferences and width calculation; implement the shared column model and make tests pass.
- [x] 1.2 Add the translated Columns picker with persistent desktop/mobile choices; verify checkbox behavior, reload, reset and keyboard dismissal in the browser.

## 2. Layout and verification
- [x] 2.1 Give header and rows one shared width; verify horizontal reachability and alignment of the last column at narrow and wide sizes.
- [x] 2.2 Run fresh typecheck, focused tests, localization parity and strict OpenSpec validation after the final edits.
