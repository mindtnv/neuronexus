## Implementation
- [x] 1. Verify existing builtin clone/isolation and editor draft tests; update the note-type list/editor and visible card-types navigation without changing safe mutation semantics.
- [x] 2. Share card editing/preview between standalone and dock, preserving unsaved preview, draft recovery and return-to-review.
- [x] 3. Remove focus mode, relocate review actions/similar cards and restyle completion; verify no answer spoilers or changed grading semantics.
- [x] 4. Update Mermaid palette behavior with regression coverage, inline-code/blockquote typography and toast presentation.
- [x] 5. Add the lowest-priority code-execution backlog entry, update project guidance and complete fresh type/spec/tests plus browser checks.

## Follow-up feedback (2026-09-20)
- [x] 6. Replace the home garden widget with study progress, remove the deck custom-study shortcut and modernize empty/draft screens while retaining recoverable drafts.
- [x] 7. Open narrow review details in a modal, unify keyboard/action controls and show actual session analytics directly at completion; retire the separate summary screen.
- [x] 8. Simplify type authoring: icon-only entry, no clone notice/interstitial, modal answer-mode settings, readable conversion and bulk-selection actions; preserve scope and confirmation guards.
- [x] 9. Add corner-origin theme transitions with reduced-motion and unsupported-browser fallback, and correct archive/type icons.
- [x] 10. Verify follow-up interactions, accessibility, session totals and safe mutation semantics with fresh tests, typecheck, specs and browser checks.

## Dropdown, spacing and results follow-up
- [x] 11. Match select popovers to the active palette, reduce the shell gutter and remove redundant manual controls while waiting for a scheduled learning step.
- [x] 12. Add accessible grade-distribution and answer-duration charts from successful session history; handle a single answer and bounded grouping honestly, then verify tests/types/specs and browser layouts.

## Context actions and graph legend
- [x] 13. Add card context actions for one or selected cards, with keyboard and visible touch access; preserve confirmations and server-authoritative selection.
- [x] 14. Make the graph legend collapsible, initially collapsed on mobile, retaining cluster filters; verify interactions and responsive layouts.

## Settings follow-up
- [x] 15. Rework every settings section into responsive, bounded forms with local navigation, readable save/retry state and separated data controls; retain failed drafts, support keyboard retention changes, validate presets and prevent duplicate token requests. Verify regression tests, types/specs and desktop/mobile screens.
