# Tasks

## 1. Secure tool integration
- [x] 1.1 Add failing registry/ownership/preview/rollback tests, implement knowledge adapters, and make focused tests pass.
- [x] 1.2 Persist confirmation previews, reject stale state and replay, and run source post-commit hooks only after commit; verify confirm/resume integration tests.
- [x] 1.3 Add current-registry guidance, complete localized tool labels and durable preview rendering; verify registry uniqueness and reload tests.

## 2. Presentation and verification
- [x] 2.1 Compact completed read steps and clarify pending confirmations; verify browser behavior without hiding approvals.
- [x] 2.2 Remove the reveal stripe and add minimally rounded code blocks with line numbers and copying; verify code fidelity, idempotence, sanitization and browser copy.
- [x] 2.3 Run fresh typecheck, focused API/web tests, MCP regressions and strict OpenSpec validation; document the resulting tool inventory and limitations.
